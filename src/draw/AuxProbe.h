//
//	draw/AuxProbe.h
//
//	【一時計装 ── 役目を終えたら消す】グラフィック凡例の**per-instance の設定がどこに
//	保存されているか**を実機で突き止めるための、読み取り専用のダンプ。図面は一切変更しない
//	（docs/DEV-NOTES.md「実機確認の作法」の「役目を終えた計装は消す」）。
//
//	【1 度目（#86）で分かったこと】「ビューポートでフィルタ」は凡例にぶら下がる
//	**データオブジェクトのタグ付きデータ**（容れ物 `'GrLg'` ・型 15 ・タグ 5）にあった。
//	この計装はそれを突き止めるために作られ、役目を終えて #89 で外した。
//
//	【2 度目で分かったこと】per-instance の設定の在り処が 3 つ割れた（docs/DEV-NOTES.md
//	「グラフィック凡例」に記録済み）:
//	  * **ソース定義は `'GrLe'`**（22 バイト。手で設定した凡例にだけ現れる）
//	  * **フィルタは何件でも指定できる**（`'GrLg'` ・型 15 ・タグ 5 の配列の要素数が増える）
//	  * **`ImageScale` は OIP の「イメージの縮率」ではない**（縮率はレコードの外）
//	同時に、**タグ付きデータを型で総なめするのは無駄**だと分かった——
//	`TaggedDataGetNumElements` は渡した型 ID を検証せず、どの型でも同じ件数とゴミの値を返す。
//	そこでこの計装は総なめをやめ、**生バイト（16 進＋16 ビット）**を出すことに徹する。
//
//	【3 度目に追うもの】22 バイトのどこが何か。**差分を取る**のがいちばん早い——検索条件だけ
//	変えた凡例・イメージの縮率だけ変えた凡例を同じ文書に置いて 1 回走らせれば、動いた
//	バイトがその設定の在り処になる。
//
//	【何を出すか】**文書内のグラフィック凡例すべて**について
//	  * パラメトリックレコードの全欄（universal 名・OIP の表示名・フィールド種別・値）
//	  * 「ビューポートでフィルタ」の指定先（**型とタグが分かっているので実体まで解ける**。
//	    複数指定でも全部出る）
//	  * 補助オブジェクトの連鎖（型・4 文字タグ・埋め込みの容れ物 ID・16 進ダンプ・
//	    **+86 から先を 16 ビットで読み直した並び**）
//	を 1 つの文字列にする。**手で設定した凡例と素の凡例を同じ文書に置いて 1 回走らせれば、
//	差分がそのまま「その設定の持ち方」になる。**
//
//	【dev ビルド専用】stable では常に空文字を返す（計装をユーザーの手元で動かさない）。
//
//	【SDK 型を公開しない】戻りは std::string だけなので、draw/DrawUtil.h は要らない
//	（＝SDK を持たない翻訳単位からも include できる。DrawUtil.h 冒頭の約束を破らない）。
//

#pragma once

#include <string>

namespace HomeskzIfcImport::draw
{
	// 文書内のグラフィック凡例をすべてダンプする。**何か 1 つでも選ばれていれば**働き、
	// 選択が無い／stable ビルドのときだけ空文字を返す（呼び出し側はそのまま通常の処理を
	// 続ける）。
	//
	// **「選択が凡例なら」で絞らない。** 実機で空振りした（凡例を選んでいたのにインポートが
	// 始まった）ためで、絞りを厳しくするほど原因が見えなくなる。凡例かどうかの判定結果と、
	// 実際の登録名はダンプの先頭に書き出す。
	std::string probeSelectedLegendAuxData();
} // namespace HomeskzIfcImport::draw
