# ROADMAP — ホームズ君 IFC インポート C++ 移植

Python プラグイン（`vectorworks-plugin-script-import-ifc-homeskz`）を C++ SDK ネイティブ
プラグインへ移植するための計画。設計原則・規約は `CLAUDE.md` を参照。

## 進め方の原則

- **1 マイルストーン = 1 縦切り**。各マイルストーンは
  「IFC パース → 命令セット（Document）→ VW 描画 → **ローカル VectorWorks で目視確認**」
  の 1 周が回る、単体で価値のある増分にする。
- **小さく積む**。大きな要素（基礎・横架材）はサブマイルストーンに割る。
- **依存順に進む**。土台（幾何・ストーリ）を先に作り、それに乗る要素を後に。
- **形状を先に確定し、支持部材を形状へ合わせる**（下記「実装順序の方針」）。
- 実描画の正否は**ローカルでしか確定できない**。仕様に迷いが出たら、次へ進む前に
  Python 版 CLAUDE.md の該当節と実装・テストを読み、必要ならユーザーに確認する。

**本ロードマップに出てくる VectorScript 名（`Move3D` / `BeginRoof` / `DoubLines` /
`BeginXtrd` 等）は「何をするか＝意図」の表記であり、呼ぶべき API の確定ではない。**
ISDK には VS の一部（3D 変換状態・屋根作成の一連・レイヤ高さ取得など）が無く、M6 では
配置行列（`CreateCustomObjectByMatrix`）と屋根面オブジェクト（`VWRoofFaceObj`）へ
置き換えた。着手時に `scripts/ci-debug.sh`（`--mode sdk-grep`。CLAUDE.md「CI デバッグ」）で
実在 API を確認し、無ければ SDK の作法へ読み替える。

各マイルストーンの完了条件（Definition of Done）:

1. `parse/` の解析＋`core/` の型が**無 SDK で単体テスト**を通る（実 IFC フィクスチャ）。
   併せて**全フィクスチャで `buildDocument` が例外なく通る**こと（Python 版
   `test_integration.py` に相当する回帰）を確認する。期待値は**手書き**で持つ
   （Python 版出力との自動突き合わせは行わない。下記「Python 版出力との比較はしない」）。
2. `draw/` が SDK ビルドでコンパイルできる（SDK API の実在は上記 ci-debug で事前確認）。
3. **ローカル確認チェックリスト**をユーザーが VectorWorks 上で確認済み。
4. lint（`scripts/lint.sh`）通過・CI green。

**Python 版出力との比較はしない。** Python 版は**仕様の一次資料**であって出力の契約ではない
（CLAUDE.md 方針1「完全一致は非目標」）。実際 M5/M6 の時点で意図的な差——床＝スラブ化に伴う
`styleName`/`components`/`datum`/`elevation` の意味、垂木・野地板の高さの渡し方、屋根組レベルを
屋根版のある階だけに絞る点——があり、以降も「Python 版のトリッキーな実装を C++ SDK の作法へ
読み替える」たびに増える。突き合わせを自動化しても**除外リストが育つだけで、差分が移植ズレか
意図的な差かの判断は人間に戻る**ため、費用対効果が合わない。移植ズレは Python 版の該当節・
実装・テストを読んで**手書き期待値へ写す**ことで防ぐ。

進捗記号: ⬜ 未着手 / 🟨 進行中（コード実装済み・ローカル目視確認待ち） / ✅ 完了

## 現在の進捗（サマリ）

この表が進捗の**単一の真実**。個別の到達内容は各マイルストーンの節に書き、ここには重ねない。

| マイルストーン | 状態 | 備考 |
| --- | --- | --- |
| M0 基盤整備 | ✅ 完了 | 骨組み・CMake 分割（無 SDK `HomeskzIfcCore`＋SDK 依存 `draw/`）・STEP リーダ・Loader・無 SDK テスト、メニューの器（ファイル選択→parse→件数ダイアログ）。PR #4〜#10。開発基盤として CI の SDK ビルド高速化（PR #15）と**手動ディスパッチ専用の CI デバッグ**（PR #20・#21）も整備済み。|
| M1 通り芯 | ✅ 完了 | 最初の縦切り。`parse/Grid`＋`draw/Grid`＋テスト（PR #11）。**ローカル目視確認済み**。|
| M2 幾何の土台 | ✅ 完了 | 配置行列・押し出し・断面（`Mat4` 含む）。`core/Geometry`＋`parse/IfcGeometry`（PR #13）。描画なし＝テストのみで完了。|
| M3 ストーリ | ✅ 完了 | 階・レベル・レイヤ（基本レベルのみ）。`parse/Story`＋`draw/Story`（PR #14）。**ローカル目視確認済み**。デザインレイヤの重ね順変更は ISDK に無く**見送り**（M13 へ委譲。M3 の節参照）。屋根組・登り梁・span 柱レベルは後続 M で追加。|
| M4 構造クラス判定 | ✅ 完了 | `parse/StructuralClass`＋Python 版テスト移植（PR #16）。基礎クラス（立上り／底盤）は M9 で定義。|
| **M5 床板（形状先行①）** | ✅ 完了 | 建物形状①。各階 `n-FL` にスラブで床を描き段差を表現（PR #18）。併せて STEP の日本語文字エスケープをデコード。**ローカル目視確認済み**。|
| **M6 屋根面・屋根組（形状先行②）** | ✅ 完了 | 建物形状②。屋根版から垂木・野地板を導出し屋根面を確定（PR #19）。**ローカル目視確認済み**。|
| **M7 横架材（支持部材）** | ✅ 完了 | 土台・梁・桁＋母屋・棟木・登り梁。M5/M6 の床・屋根面へ合わせて補正（登り梁の屋根スナップ）。**ローカル目視確認済み**。|
| **M8 柱（支持部材）** | ✅ 完了 | 管柱・通し柱・小屋束。span レイヤ分割（上階梁下端＝M7 参照）。登り梁の端部詰めを柱面へ最終化。**ローカル目視確認済み**。|
| M9 基礎（壁・スラブ）＋基礎ストーリ | 🟨 進行中 | 立上り＝壁・底盤＝スラブ・基礎ストーリ。上部形状と独立。**ローカル目視確認待ち**。|
| M10 基礎の高度化 | 🟨 進行中 | 地中梁・人通口・壁結合を実装し**ローカル目視確認済み**（PR #41）。残りは**配筋（保留）**のみ。|
| **M11 シンボル置換系** | ✅ 完了 | アンカーボルト・床束・火打・仕口（`parse/{AnchorBolt,FloorPost,FireBrace,Joint}`＋共通の `draw/Symbol`）。M9 の基礎ストーリへ**基礎天端・床束のレベル**を足し、配置先レイヤ（`F-アンカーボルト` / `F-床束`）も揃えた。`VWSymbolObj` は生成しただけでは図面に現れないので**配置先レイヤへ入れ直す**（M11 の節の落とし穴）。**ローカル目視確認済み**。|
| M12 断面記号・伏図記号 | ✅ 完了 | **記号 PIO を本プラグインへ同梱して実装**（`parse/ColumnMark`＋`draw/ColumnMark`＋`Extensions/ExtColumnMark`）。PIO がリセットのたびに対象レイヤの構造材を走査し、断面記号＝実断面の対角線（柱＝×・小屋束＝／）、伏図記号＝シンボルを描く。M13 の伏図へ伏図記号レイヤの表示も足した。**ローカル確認済み**。|
| M13 シート・伏図・タグ・凡例 | 🟨 進行中 | **シートレイヤ・ビューポート（基礎伏図／柱梁伏図／母屋伏図）を実装**（`parse/Sheet`＋`draw/Sheet`）。**レイヤ重ね順の目的もここで per-viewport 上書きにより満たした**。伏図記号レイヤ（切断の直下 1 枚）の表示は M12 で着地。**ローカル目視確認待ち**。残りは断面寸法データタグ・グラフィック凡例。|
| M14 断面ビューポート | 🟨 進行中 | 軸組図（`parse/Section`＋`draw/Section`）。**Python 版と違い断面ビューポートを新規作成する**（`ISDK::CreateSectionViewport`）ので、既製 40 枚の手置き・流用・削除は要らなくなった。**ローカル目視確認待ち**。|
| M15 仕上げ（実行 UX・診断） | 🟨 進行中 | **進捗表示（進捗ダイアログ＋キャンセル）を先行実装**（`core/Progress`＋`draw/ProgressDialog`）。表示とオーバーヘッドは**ローカル確認済み**。残りは例外処理・完了文言の集約（`parse/Summary` を転用）・`core/Trace`・Undo の一括化。Python 版 `run()` / `tracing.py` 相当。|

**次の一手:** M13 の伏図（シート・ビューポート）と M14 の軸組図（断面ビューポート）を
**ローカル目視確認**する（M12 の記号は確認済み）。その後 M13 の残り（断面寸法データタグ・
グラフィック凡例）。M9 のローカル目視確認は残っている（M10 / M11 の確認で
立上り・底盤・基礎ストーリも一緒に見えているので、残りは M9 固有の項目だけ）。M10 の残りは
**配筋（保留）**で、着手するときは `ifc/reinforcement.py` を移植して統合キーにも足す
（M10 の節）。

**マイルストーン番号は動かさない。** 既存のコードコメント・PR 本文が M7〜M14 の番号を参照して
いる（`src/core/Document.h` の TODO 等）。マイルストーンを増やすときは末尾に足すか非整数
（M9.5 等）にし、既存の番号は振り直さない。

## Python 版モジュールの割り当て（取りこぼし確認）

Python 版の全モジュールがどのマイルストーンに載るかの一覧。**要素の取りこぼしは無い**ことを
ここで機械的に確認する（新しい要素を Python 版に足したときは、この表にも行を足す）。

| Python | 割り当て | | Python | 割り当て |
| --- | --- | --- | --- | --- |
| `ifc/loader.py` | M0 | | `ifc/footing.py`（残り） | M10 |
| `document.py` | M0〜（各 M で拡張） | | `ifc/reinforcement.py` | M10（保留） |
| `ifc/grid.py` | M1 | | `ifc/anchor_bolt.py` | M11 |
| （ifcopenshell の幾何） | M2 | | `ifc/floor_post.py` | M11 |
| `ifc/story.py` | M3（+ 各 M でレベル追加） | | `ifc/fire_brace.py` | M11 |
| `ifc/structural_class.py` | M4 | | `ifc/joint.py` | M11 |
| `ifc/floor.py` | M5 | | `ifc/column_mark.py` | M12 |
| `ifc/rafter.py` | M6 | | `ifc/sheet.py` | M13 |
| `ifc/roof.py` | M6 | | `ifc/tag.py` | M13 |
| `ifc/member.py` | M7 | | `ifc/section.py` | M14 |
| `ifc/noboribari.py` | M7 | | `__init__.py` `run()` | M0 の器 → M15 で仕上げ |
| `ifc/column.py` | M8 | | `tracing.py` | M15 |
| `ifc/footing.py`（主要 2 種） | M9 | | `ifc/footing.py` `has_foundation` | M11 で先取り |

## 実装順序の方針（形状先行）

**建物の形状を先に確定し、それに支持部材を合わせる。** ホームズ君 IFC のうち
**床（床版）・屋根（屋根版）・屋根組**は建物の外形・面そのものを表す一次情報で、最終的な
位置決めの基準になる。そこでこれらを**先に**取り込んで位置を確定してから、**横架材・柱**という
支持部材を、確定した床・屋根面に合わせて補正しながら載せていく。

この順序は Python 版の実装順（部材が先・屋根は後）とは逆だが、Python 版でも実際には
**屋根の形状が基準**になっているため意図に合致する:

- **垂木・野地板は屋根版から導出**される（IFC に垂木は出力されない）。面が形を決める。
- **登り梁はホームズ君の入力位置が不正確**（軸勾配が屋根版より急・端部が受け材へ食い込む）
  なため、`correct_noboribari` で天端中央線を屋根面（垂木下面）へスナップし端部を詰める。
  つまり登り梁は「屋根面へ補正される支持部材」。
- **床（床版）は IFC の床位置を尊重**してスキップフロアの段差を絶対 Z で表す。床の位置
  そのものが一次情報。

屋根面より先に部材を置いてから屋根に合わせ直す Python 版の後処理（`correct_noboribari`）を、
C++ では**屋根面を先に確定した上で**素直に適用できる。

**依存メモ:**

- 垂木・野地板は屋根版と**ストーリ（M3）の横架材天端レベル**だけに依存し、横架材（梁）には
  依存しない。垂木の差し込み（`embedment`）は真下の軒桁幅を参照する。M6 の時点では既定桁幅
  （`kDefaultGirderWidth`＝105mm）へフォールバックしており、M7 で実寸へ切り替えた（受ける
  軒桁が見つからないときのフォールバックとしては今も同じ値を使う）。
- 登り梁は横架材（`IfcBeam`）の一種なので**パーサは横架材（M7）と一体**。M7 で任意断面を抽出し、
  M6 で確定済みの屋根面へスナップ補正する（実装済み。端部詰めは M7 の時点では受け材＝横架材
  のみを見ており、柱を参照する最終化は M8 で実施済み）。母屋・棟木も横架材の一種で M7 で
  `n-母屋` へ分離した。

---

## M0 — 基盤整備（骨組み・STEP リーダ・Document 土台）✅

**目的:** テンプレートを本プラグインへ改名し、2 フェーズの骨組みと無 SDK テスト土台を敷く。
描画対象はまだ無し（土台のみ）。

- ✅ プレースホルダー識別子の置換（`SamplePlugin` → `HomeskzIfcImport`。バンドル名・`.vwr`・
  VCOM ユニバーサル名・**UUID を再生成**・namespace（`CExtMenuImportIfc` /
  `CImportIfcMenu_EventSink`）・`VW_REPO`・メニュー `ファイル` ▸ `IFC (ホームズ君) 取り込み…`）。
  現在の識別子は README「プラグイン識別子」節を参照。`.vwstrings` は UTF-16LE/BOM/CRLF を保持。
- ✅ ディレクトリ骨組み: `src/core/` `src/parse/` `src/draw/` と CMake ターゲット分割。
  **`parse/`・`core/` は VectorWorks SDK を include しない**ビルド構成（無 SDK 静的ライブラリ
  `HomeskzIfcCore`。プラグインは SDK 依存 `draw/` を上乗せ、テストは同ライブラリを無 SDK で
  リンク。PR #5）。
- ✅ `core/Geometry.h`: `Vec2` / `Vec3`（M0）と `Mat4`・ベクトル演算（M2）。
- ✅ `core/Document.h`: Document 構造体（バージョン＋各命令リストの器）と `validateDocument`
  の骨組み（PR #5）。命令リストは各要素マイルストーンで追加。
- ✅ `parse/Step`: **最小 STEP リーダ**（`Model` の `entity` / `byType` / `referrers` /
  `resolve`＋`parseStep`）。`StepTests` で網羅（PR #6）。
- ✅ `parse/Loader`: ファイル読み込み（`loadIfc` / `loadIfcFromText`）。自前リーダは非正規
  エンティティ（IFC4 専用 `IFCFOOTINGTYPE` の混入等）を許容するため、Python 版 `loader.py` の
  ようなサニタイズは不要（詳細は CLAUDE.md「アーキテクチャ」）。`LoaderTests`（PR #6）。
- ✅ `tests/`: ホームズ君の実 IFC フィクスチャを Python 版から一式流用（PR #8）。
- ✅ メニューコマンドを「IFC をインポート」の器へ改修（ファイル選択 → `summarizeIfc` →
  件数ダイアログ。PR #10）。**`summarizeIfc` は M1 で `buildDocument` に置き換わり、以後
  未使用**。M15 で完了文言の組み立てへ転用する。
- ✅ 開発基盤（本マイルストーン外だが M0 の延長）: CI の SDK ビルド高速化（PR #15）、
  **手動ディスパッチ専用の CI デバッグ**（`ci-debug.yml`＋`scripts/ci-debug*.sh`。PR #20・#21）。
  リモートセッションには SDK が無いため、SDK API の実在確認・SDK ビルドの再現はここで行う
  （CLAUDE.md「CI デバッグ」）。

**ローカル確認:** ✅ メニューからコマンド実行 → IFC を選ぶ → 件数がダイアログに出る。

---

## M1 — 通り芯（グリッド）★最初の縦切り ✅

**目的:** 2 フェーズが端から端まで通ることを、最も単純な要素で実証する。
grids は配置行列・断面・ストーリを必要としない（`IfcGridAxis` → ポリライン → 中心
オフセット → `GridAxis` オブジェクト）。**Python: `ifc/grid.py` / `vw/grid.py`。**

- ✅ `parse/Grid`: `IfcGridAxis` の `AxisCurve`(`IfcPolyline`) の全点を取り連続点対
  （区間）ごとに 1 本、重複線除去（反転も同一）、bbox 中心でセンタリング、X/Y 通り
  判定（名前 `X`/`Y` 始まり優先、無ければ `|Δx|<|Δy|`）、クラス名付与 → `GridCommand`。
  **Python 版 `ifc/grid.py` と整合**（クラス名・区間分割・非ポリライン曲線のスキップ）。
- ✅ `core/Document`: `GridCommand`（`label` / `layer='共通'` / `drawClass` / `start` / `end`）
  と `Document.grids`。`validateDocument` に通り芯の検証（レイヤ名・非縮退）を追加。
- ✅ `draw/Grid`: `共通` レイヤを（無ければ）作成し、`CreateCustomObjectPath('GridAxis', …)`
  で生成。失敗時は通常線にフォールバック。`Label` / `ShowBubbleAt='Start Point'` を設定。
- ✅ `draw/ExecuteDocument`: grid 命令だけをディスパッチする最小版。メニューコマンドを
  `buildDocument`→`executeDocument`→本数ダイアログの流れへ改修。
- ✅ テスト: `ParseGridTests`（本数・センタリング・X/Y 判定・重複除去・区間分割・
  非ポリラインスキップ・欠損スキップ・決定性）。
- ✅ **ローカル目視確認済み**。`共通` レイヤに通り芯が描かれ X/Y でクラス分け・軸名ラベル・
  基点バブルが出て、原点付近にセンタリングされることを VW 実機で確認。

---

## M2 — 幾何の土台（配置行列・押し出しソリッド・断面） ✅

**目的:** M3 以降のほぼ全要素が使う共有の幾何計算を、描画なしで先に固めて de-risk する。
**Python: ifcopenshell 依存部の自前計算（`ifc/footing.py` の `_world_solid` 等）＋
`ifc/member.py` の `_get_placement_3d` / `_get_profile_dims`。**

- ✅ `core/Geometry`: `Vec2`/`Vec3` の演算と `Mat4`（単位・平行移動・基底からの生成・
  行列積・点/方向の適用）。剛体変換（回転＋平行移動）のみで剪断・スケールは持たない。
- ✅ `parse/IfcGeometry`: `IfcAxis2Placement3D`（Gram-Schmidt で正規直交化）→ 変換行列。
  要素配置 `resolveObjectPlacement` は **RelativePlacement のみ**を使い親を合成しない
  （Python 版と一致。親を合成すると階高が二重計上される）。
- ✅ 押し出しソリッド（`IfcExtrudedAreaSolid`）のワールド変換 → `WorldSolid`
  （配置基底・押し出し方向／長さ・プロファイル 2D 頂点・矩形寸法を保持）。鉛直・水平いずれの
  押し出し方向でも同じ経路で正しく変換される（テストで両方を検証）。
- ✅ 断面プロファイル: `IfcRectangleProfileDef`（`Position` は平行移動のみ）と任意閉断面
  `IfcArbitraryClosedProfileDef`（`OuterCurve` の外形）。
- ✅ 差演算 `IfcBooleanResult` / `IfcBooleanClippingResult` の第 1 オペランドを再帰で辿る。
- ✅ テスト: `GeometryTests`。行列・配置・断面・押し出し・boolean 辿りを手計算値と
  突き合わせ、実フィクスチャで数百の押し出し・断面・boolean が例外なく解決できることを確認。

**ローカル確認:** （描画なし。テストのみ。）

---

## M3 — ストーリ（階・レベル・レイヤ） ✅

**目的:** 以降の要素の配置先となるストーリ・ストーリレベル・デザインレイヤを生成する。
**Python: `ifc/story.py` / `vw/story.py`。**

- ✅ `parse/Story`: `…FL` で終わる `IfcBuildingStorey` を対象に、`Elevation`＝ストーリ高さ、
  `IfcColumn`/`IfcSlab` のローカル Z 最大値（≤0）＝横架材天端オフセットを算出。
  階名・suffix・レイヤ名・レベル（`FL`/`横架材天端`、最上階 `軒高`）を組み立て → `StoryCommand`。
  Elevation 昇順（同値は #id 昇順で安定）・列挙順に依存しない決定値。屋根組（M6）・
  登り梁・母屋（M7）・span 柱（M8）レベルは各要素の導入時に追加する。
- ✅ `core/Document`: `LevelCommand`/`StoryCommand`＋`Document.stories`。
- ✅ `draw/Story`: `CreateStory`→`SetStoryElevation` を 1 階ずつ。レベルは
  `CreateLayerLevelType`＋`CreateStoryLevelTemplate`＋`AddStoryLevelFromTemplate`＋
  `GetLayerForStory`＋`SetObjectName`。
- ✅ テスト: `ParseStoryTests`（Z 抽出・オフセット最大値・階数・レベル・センタ判定・
  非 FL 除外・希望レイヤ順・決定性）。
- ✅ **ローカル目視確認済み**。ストーリと各レイヤが階数分でき、レベル高さが正しいことを
  VW 実機で確認。

**【決定】デザインレイヤの重ね順は並べ替える（M13 で確定。ここが唯一の決定箇所）。**
並べ替えの目的は「伏図で床・野地板が柱・梁を覆わない」こと。希望順の**計算**は SDK 非依存の
`core::desiredStoryLayerOrder`（`共通` 先頭・最上階→最下階・床/野地板は背面）として core に
実装済み・無 SDK テスト済みで、**適用は `draw/Story` の `reorderStoryLayers` が唯一の箇所**。

M3 の時点では「ISDK に重ね順を変える呼び出し（VS の `HMoveForward` 相当）が無い」と判断して
per-viewport の `SetViewportLayerStackingOverride` に委ねたが、**M13 の実機確認でこれは
2 点とも誤りだった**:
  * per-viewport 上書きは**効かない**——呼び出しは true を返すのに
    `GetNumViewportLayerStackingOverrides` は 0 のままで、OIP も「順序を上書き: いいえ」。
  * ISDK には **`InsertObjectAfter` / `InsertObjectBefore`** があり、**レイヤは図面の
    オブジェクト列に並んでいる**（`VWDocument::GetDrawingHeaderFristMember` + `NextObject` で
    辿れる）。これが VS の `HMoveForward` に当たる。
したがって Python 版と同じ「ドキュメントの重ね順を並べ替える」方式に戻す。**並べ替えは
ビューポート生成より前**に行う（ビューポートは生成時の重ね順で描かれる）ので、Python 版が
「並べ替えても既存ビューポートに反映されず手動更新が必要」と VW の制約として許容していた
問題は起きない。以降の M5/M6/M13 はこの決定を参照するだけで、同じ議論を各所に書かない。

---

## M4 — 構造クラス判定（純ロジック） ✅

**目的:** 柱・横架材の `04構造-02木造-…` クラス割り当てを純ロジックとして移植。
描画は無し（M7/M8 が使う）。**Python: `ifc/structural_class.py`。**

- ✅ `parse/StructuralClass`: 種別トークン→クラスの対応表（`memberTypeOfName` /
  `memberClassFromName`。床小梁・床大梁・甲乙梁→床梁、登り梁 等）、名前で判別できない
  ときの状況推定（`resolveMemberClass`＝階・軒高、`resolveColumnClass`＝最上階・貫通判定）。
  `CLASS_*` 定数を Python 版と一字一句合わせる。**純粋な文字列／整数ロジックのみ**で STEP
  グラフにも SDK にも依存しない。基礎クラス（立上り＝`04構造-01基礎-03立ち上がり`／
  底盤＝`…-02基礎スラブ`）は Python 版では `ifc/footing.py` 側にあるが、本移植はクラス名の
  定義をここへ集約する方針なので M9 でこのファイルへ追加した（`CLASS_FOUNDATION_WALL` /
  `CLASS_FOUNDATION_SLAB`）。
- ✅ テスト: `tests/ParseStructuralClassTests.cpp`（Python 版 `test_ifc_structural_class.py`
  の全ケースを 1 対 1 で移植。フィクスチャ不要）。

**ローカル確認:** （描画なし。M7/M8 の描画時にクラス分けで確認。）

---

## M5 — 床板（形状先行①） ✅

**目的:** **建物形状を先に据える第一歩。** 各階 `n-FL` にスラブで床を描き、段差
（スキップフロア）を表現。床の位置は IFC の一次情報で、以降の高さ基準の目安になる。
**Python: `ifc/floor.py` / `vw/floor.py`。**

- ✅ `parse/Floor`: `床版`(`IfcSlab`) → `FloorCommand`（**床仕上げ上端**の絶対 Z ＝ FL ± 段差、
  FL レベルバインド＋段差 offset、スラブ構成＝床仕上げ〈FL−横架材天端−24〉＋床下地〈24〉）。
  スラブ構成は**階ごとのスラブスタイル**（`1F-床スタイル` … `屋根-床スタイル`）として持つ
  （階により構成が異なることが多いため）。平面外形・最下端 Z の抽出は M2 の `WorldSolid` を
  再利用。座標は通り芯と同じグリッド中心オフセット。
- ✅ **屋根階の床＝ロフト（小屋裏収納）**にも対応。ロフトの標準床レベルは「軒高 + 36mm」の
  **仮定値**（IFC に無い）で、構成は 床仕上げ 12 ＋ 床下地 24。高さ基準は一般階が
  **床仕上げ上端（FL レベル）**、ロフトは**床下地下端（軒高レベル＝横架材天端）**
  （ロフトの FL は仮定値なので確かな構造面を基準にする）。`parse/Story` はロフトの床がある
  屋根階にだけ FL レベル（軒高 +36）とレイヤ `R-FL` を足す（空の FL レイヤを作らない）。
- ✅ **ロフトの外形は床梁から合成する**（`core/Region` の `filledUnionOutlines`）。
  ホームズ君は小屋伏図に床梁（`木梁:床大梁` / `木梁:床小梁`）を出力する一方、**ロフトの
  床版は出力しない**（実データで確認: 屋根階に `床版` を持つモデルは 1 件も無い）。そこで
  屋根階に床版が無いときに限り、床梁が**囲む**領域——軸並行セル分割で骨組みの内側の空隙を
  求め、その空隙とそれを囲う梁——を床の外形とみなす。根太が渡っていない（何も囲っていない）
  単独の床梁は床にしない（外形に部材幅のヒゲが生えない）。**近似であること**: 床梁で
  囲まれた吹抜けは塞がり、骨組みが閉じていない部分は床にならない。床版があるときは常に
  そちらを使う。
- ✅ `parse/IfcGeometry`: **`IfcArbitraryProfileDefWithVoids`（開口付き断面）に対応**。
  階段の吹抜けを持つ床版はこの型で出力され、未対応だと**床が丸ごと落ちていた**
  （実データ 2 件で 2 階・3 階の床が入っていなかった）。外形（`OuterCurve`）だけを読み、
  開口（`InnerCurves`）は無視する＝床は開口を塞いだ形で入る。**開口の再現は後続課題**
  （VW スラブへ穴を開ける API の調査が必要）。
- ✅ `parse/IfcGeometry` 拡張: 要素の形状表現から押し出しを取り出す `firstExtrudedSolid` /
  `resolveElementWorldSolid`、平面外形 `footprint`（鉛直押し出し＝プロファイル、水平押し出し＝
  掃引矩形）、Z 範囲 `zTopAndThickness`（Python 版 `ifc/footing.py` の低レベルヘルパー相当。
  M9 基礎でもそのまま使う）。`parse/Grid` にセンタリング中心を返す `gridCenterOf`（解析中は `parse/Context` が共有）、
  `parse/Story` に階の収容要素を返す `collectStoryElements` を追加。
- ✅ `parse/Step` 拡張: **ISO 10303-21 の拡張文字エスケープ（`\X2\…\X0\` / `\X\HH` /
  `\S\c` / `\P?\`）を UTF-8 へデコード**。ホームズ君 IFC の日本語 `Name`（`床版` 等）は
  UTF-16 エスケープで出力されるため、これが無いと名前による要素判別が一切通らない
  （ifcopenshell が内部で行っているのと同じ扱い。M7 以降の `木梁:…` 等の判定にも必須）。
- ✅ `core/Document`: `FloorCommand`（`components` ＝ スラブ構成層）＋ `SlabComponentCommand`
  ＋ `StoryBoundCommand`。`validateDocument` に床の検証（外形 3 点以上・構成層・総厚 > 0）。
- ✅ `draw/Floor`: 外形の閉じたポリゴン→`CreateSlab`→クラス・by-class 属性→**階ごとの
  スラブスタイル**を用意（無ければ作成）して構成層（床仕上げ／床下地の厚み・名前）を
  設定し適用→`SetSlabHeight`（**天端**＝床仕上げ上端の絶対 Z）→`SetObjectStoryBound`
  （`FL` レベルへ段差 offset でバインド）→`ResetObject`。余った構成層は厚み 0 に潰さず
  **削除**する（厚み 0 の層は VW が受け付けないため）。配置先 `n-FL` レイヤが無い命令は
  スキップ（レイヤは story 命令が作る）。スラブを作れない場合は外形ポリゴンへフォールバック。
- ✅ **描画オブジェクトはスラブ**（Python 版の床ツールから意図的に変更）。床ツールは実体が
  押し出しの派生でオブジェクト構造が押し出しとほぼ変わらないのに対し、スラブは BIM
  オブジェクトとして機能（コンポーネント・スタイル・データ連携）が強化されており発展性が
  高い。ISDK にも Floor の生成 API は無く Slab には一式が揃っているため SDK の作法にも
  素直に乗る。描画手順は Python 版 `vw/footing.py` の `draw_slab`（底盤＝M9）と共通の作法。
- ✅ テスト: `ParseFloorTests`（`test_ifc_floor.py` の全ケースを移植: 枚数・レイヤ振り分け・
  厚み 24 固定・クラス・`elevation = 横架材天端 + offset` の不変条件・スキップフロアの段差
  −832mm・横架材天端より上の床・センタリング済み外形・決定性。合成モデルでの抽出条件も追加）。
  `StepTests` に文字エスケープのデコード、`CoreDocumentTests` に床の検証ケースを追加。
  `CoreRegionTests`（領域合成: 閉じた骨組み・内部の根太・突き出しの除外・連結成分・縮退）、
  `ParseFloorTests` にロフト床の合成ケース、`GeometryTests` に開口付き断面のケースを追加。
- ✅ **ローカル目視確認済み**（下記チェックリストを VW 実機で確認）。
- ✅ ロフト床の合成・開口付き断面の対応も**ローカル目視確認済み**（下記「ローカル確認（追加・済）」）。

床・野地板レイヤを最背面へ回す件は **M3 の【決定】**（重ね順は変更せず M13 の per-viewport
上書きで満たす）に従う。

**ローカル確認（済）:** 各階の `n-FL` レイヤに床（スラブ）が描かれる。**床仕上げ上端が FL**
（床レベル指定のある床は FL ± 差分）に一致し、スラブ構成が上から `床仕上げ`
（FL−横架材天端−24）＋`床下地`（24）になっている（＝スラブ下端が横架材天端に一致）。
スラブスタイル（`1F-床スタイル` 等）が階ごとに 1 つでき、床がそれを参照している。2D 表現が
床ツールと異なる点も確認。スキップフロアの段差が高さの差として表れる
（`スキップフロア_サンプル` の 2FL は通常床と 832mm 下がった床が混在）。床を選択して OIP の
高さ基準が `FL` レベル＋offset になっており、編集しても高さがずれない。

**ローカル確認（追加・済）:** 屋根階に床梁のあるモデルで `R-FL` レイヤにロフト床が
描かれ、その範囲が小屋伏図の床梁が囲む範囲と一致する（床下地下端＝軒高）。開口
（階段の吹抜け）を持つ床版の階——`グレー本モデルプラン2【3階】` の 2 階・3 階、
`伏図次郎【2階】` の 2 階——で床が入る（開口は塞がった形になる）。

---

## M6 — 屋根面・屋根組（垂木・野地板）（形状先行②） ✅

**目的:** **建物形状の要＝屋根面を確定する。** 屋根版（`IfcSlab` の屋根面）から垂木・
野地板を導出し、屋根面の勾配・外形を据える。これが M7 の**登り梁を屋根面へスナップ補正**
する際の基準になる（形状 → 支持部材の依存を作る）。母屋・棟木・登り梁（＝横架材）は
M7 で扱う。**Python: `ifc/rafter.py` / `ifc/roof.py`（＋屋根面ヘルパー `_roof_plane`）。**

- ✅ `parse/IfcGeometry` 拡張: 屋根版から**屋根面**（平面外形頂点列＋上向き単位法線）を
  取り出す `roofPlane`（Python `rafter._roof_plane`）。M2 の `resolveElementWorldSolid` の
  配置基底＋プロファイル頂点をそのまま使う。M7 の登り梁スナップも同じ関数を使う。
- ✅ `parse/Rafter`: 屋根版から垂木を導出（`n-垂木`、既定 45×45・間隔 455mm・クラス
  `…05小屋組-05垂木`）。勾配方向（法線の水平成分）へ流し、直交方向へ掃引（**両端は屋根面の
  端から半幅内側**・内部は 455mm 以下＝中間ちょうど・端数は両端へ等分）、走査線法で外形を
  クリップ（非凸面＝ドーマも分割）、支持点＝屋根面と横架材天端（最上階は軒高）Z の交点、
  軒の出（＝支持点→軒先 − 差し込み）・差し込み・仕様ラベル `45×45@455` → `RafterCommand`。
  **差し込み桁幅は横架材未導入のため既定桁幅 105mm にフォールバック**（M7 導入後に精緻化。
  Python 版も `members` 空のとき同値）。
- ✅ `parse/Roof`: 屋根版 1 面＝野地板 1 枚（`n-野地板`、厚み 12mm 固定、クラス
  `…06耐力面材-03屋根`）→ `RoofCommand`（軒＝最も低い頂点を通る屋根軸・upslope 定義点・
  勾配 rise/run＝法線の水平/鉛直成分・軒の絶対 Z＝屋根版の平面 ＋ 垂木せいの鉛直換算）。
- ✅ `parse/Story` 拡張: **屋根版を持つ階**に `垂木`/`野地板` レベル（`n-垂木`/`n-野地板`
  レイヤ）を横架材天端（最上階は軒高）の直上へ追加（`storyHasRoofSlab`＝Python
  `story_has_roof`）。Python 版は最上階に無条件で足すが、本移植は**空レイヤを作らない**方針で
  屋根版のある階だけに絞る（実データでは最上階は必ず屋根版を含むので結果は一致）。
- ✅ `core/Document`: `RafterCommand`/`RoofCommand`＋`Document.rafters`/`.roofs`。
  `validateDocument` に垂木（レイヤ・クラス・断面が正・平面が非縮退）と野地板
  （レイヤ・クラス・外形 3 点以上・厚みが正）の検証を追加。
- ✅ `draw/Rafter`: **配置行列**（`VWTransformMatrix` で Z 軸まわりに平面方位角＋支持点への
  平行移動）から `CreateCustomObjectByMatrix` で軸組ツール（`FramingMember` type=`rafter`）を
  生成 → クラス・by-class 属性 → 断面／`LineLength`／`PitchAngle`／`verticalReference`／
  `2DDisplay`／軒の出／差し込み（登録名 `bearinginset`）／ラベル（`showLabel` ＋ `labelText`）／
  構造用途（`structuralUse`）／材質を設定 → `ResetObject`。生成できなければ平面投影の直線に
  フォールバック。**パラメータ名は VW 実機の PIO 登録から採る**（当初 VectorScript
  エクスポートから推定した名前——`pitch`・`label`・先頭大文字の `StructuralUse`——では
  setter が**黙って無視されていた**。`draw/Rafter.cpp` 冒頭）。生成時に設定ダイアログが
  開かないよう `DefineCustomObject(…, kCustomObjectPrefNever)` を 1 度だけ呼ぶ。
- ✅ `draw/Roof`: **屋根面オブジェクト**（VWFC の `VWRoofFaceObj`）を外形ポリゴンから生成し、
  **屋根軸・棟側の点・勾配・軸の Z はオブジェクト変数**（`ovSlabRoofPt1/Pt2/UpslopePt/Rise/Run`・
  `ovSlabHeight`）で与えてハンドル版コンストラクタで形状を作り直す → 厚み（12mm）を
  `SetThickness` → クラス・by-class 属性 → `ResetObject`。作れなければ外形ポリゴンに
  フォールバック。**`VWRoofFaceObj(type, poly, z, upSlopeDir, rise, run)` は使えない**
  （SDK のソース上 `z` を読まず、屋根軸を原点を通る線として置く）ことと、生成しただけでは
  図面に入らない（明示的に `AddObjectToContainer` が要る）ことはローカル確認で判明した
  （`draw/Roof.cpp` 冒頭）。`ovSlabRoofUpslopePt` は**方向ではなく棟側の点**。
- ✅ **［Python 版と実現手段が異なる点］ISDK には VectorScript の 3D 変換状態
  （`ResetOrientation3D`／`Rotate3D`／`Move3D`）と屋根作成の一連の呼び出し
  （`BeginRoof`／`EndGroup`／`GetZVals`／`SetZVals`／`LNewObj`／`GetRoofFaceCoords`）が無い**
  （CI の SDK ビルドで確認）。そのため上記のとおり配置行列と屋根面オブジェクトへ置き換えた。
  副産物として、Python 版 #113 の落とし穴（テンプレートのポリゴンを屋根と誤認する／確定後の
  後付け操作で VW がクラッシュする）が構造的に起きない。`GetLayerElevation` 相当も無いため
  **高さは命令の絶対 Z をそのまま渡す**（Python 版の垂木と同じ挙動）。レイヤ相対にすべきかは
  ローカル確認項目で、`draw/Rafter` の `SetOffset`／`draw/Roof` の `ovSlabHeight` 設定の
  1 か所ずつで切り替えられるようにしてある。**この「VS 名 → ISDK の実在 API」への読み替えは
  M7 以降でも必要**（冒頭「進め方の原則」参照）。
- ✅ テスト: `ParseRafterTests` / `ParseRoofTests`（`test_ifc_rafter.py` / `test_ifc_roof.py` を
  移植。掃引割り付け・非凸面の分割・支持点と軒の出・ラベル・厚み・軒軸・勾配・センタリング・
  退化面のスキップ・レイヤ振り分け（伏図次郎＝`2-垂木`/`R-垂木`・`2-野地板`/`R-野地板`）・
  決定性）。`ParseStoryTests` に 垂木/野地板 レベルの追加条件とスタック位置、
  `CoreDocumentTests` に検証ケースを追加。
- ✅ **ローカル目視確認済み**（下記チェックリストを VW 実機で確認）。

**ローカル確認（済）:** 屋根版を含む階に `n-垂木`/`n-野地板` レイヤができ、屋根面に沿って垂木が
455mm 以下の間隔で並ぶ（両端は屋根面の端から半幅内側で、垂木がケラバからはみ出さない）。
垂木の下端が屋根版の平面に載り、軒先が「支持点＋差し込み＋軒の出」の位置に来る（差し込みが
既定 88.9mm のまま残って軒先が外へずれていないか）。**高さの基準**（垂木・野地板がレイヤ高さぶん
二重に持ち上がっていないか）も確認する——ISDK にレイヤ高さの取得が無いため絶対 Z で渡しており、
ずれていればレイヤ相対へ直す（上記のとおり 1 か所ずつで切り替えられる）。野地板が屋根版 1 面ごとに
1 枚描かれ、勾配が屋根版どおりで、**下端が垂木の上端に一致**する（厚み 12mm が軸のどちら側へ
伸びるかも確認する。Python 版 CLAUDE.md の文言と実装で差異がある箇所）。下屋根（中間階）でも
同様に描かれる。この屋根面が M7 登り梁のスナップ基準になる。

---

## M7 — 横架材（土台・梁・桁・母屋・棟木・登り梁）★形状へ合わせる支持部材 ✅

**目的:** 中核の支持部材。土台・梁・桁をストーリレベルにバインドして描き、**母屋・棟木・
登り梁を専用レイヤへ分離**する。**登り梁は M6 で確定した屋根面へスナップ補正**して勾配・
高さを屋根に合わせ、端部を受け材の面まで詰める（＝形状へ支持部材を合わせる）。
**Python: `ifc/member.py` / `ifc/noboribari.py` / `vw/member.py`。**

- ✅ `parse/Member`: `IfcBeam` / `IfcMember` → `MemberCommand`。配置（`memberPlacement3D`）・
  断面（`memberProfileDims`）・材種（`memberMaterialName`）から構造材 ID `{幅}×{背} - {材種}` を
  組み立て、**断面中心線を軸直交方向へ せい/2 持ち上げた天端中央線**を命令に入れる。高さは
  梁ごとの実配置（ストーリ高さ ＋ ローカル Z ＋ 補正）で、ローカル Z を取れない材だけレイヤ
  基準高さへフォールバック。始端／終端をそれぞれのストーリレベルへバインドし（`startBound` /
  `endBound`）、傾斜は offset 差として表す。軸が鉛直な材（火打）は断面種別より**先に**除外する。
- ✅ 母屋・棟木の `n-母屋` 分離と、登り梁の**任意断面抽出**（`slopedMemberGeometry` ＝
  平行四辺形 4 頂点から中心軸・幅・せい・傾斜を導出。火打＝鉛直軸・筋かい＝6 頂点は除外）＋
  `n-登り梁` 分離。登り梁は端部が直切り（鉛直面）なので**天端は XY ずらし無しで
  せい/(2·cosθ) 持ち上げる**（矩形前提の軸直交持ち上げだと天端が垂木下面に届かず、端部が
  軒側へずれて受け材との間に隙間ができる）。
- ✅ 食い込み調整 `resolveMemberInterferences`（T 字・L 字の取り合い。相互の食い込み量を
  比べて負け側だけを詰める／対称な角と傾斜梁は触らない／並び順に依存しない）。
- ✅ `parse/Noboribari`: **登り梁の位置補正**。①端部の食い込み解消（受ける横架材の面まで
  詰める。`memberPenetrationDepth` を再利用）、②天端中央線を **M6 の屋根面へスナップ**
  （`roofPlane` ＋ `RoofSlope` を垂木・野地板と共有。勾配方向が平行で外形が中点／端点を
  内包する面を選ぶ）。端部詰めは M7 の時点では受け材＝横架材のみを見ており、**柱を参照する
  最終化は M8 で実施済み**（ROADMAP.md M8）。
- ✅ `parse/Rafter` の精緻化: 垂木の差し込みが参照する**真下の軒桁幅**を、既定値 105mm から
  本 M の横架材命令由来の実寸へ切り替えた（`girderWidthAt`。垂木と平行に走る材は除外し、
  受ける軒桁が見つからないときだけ既定値へフォールバック）。M6 で先送りした唯一の項目。
- ✅ `parse/Story` 拡張: `母屋` / `登り梁` レベル（＋レイヤ）を追加。**［Python 版との差異・
  意図的］**Python 版は名前判定＋最上階への無条件追加でレベルを足すが、本移植は**実際に
  組み立てた横架材命令の配置先レイヤ**で判定する。名前では判別できず高さで母屋と推定された
  材（隅木谷木等）も確実に拾え、かつ母屋を持たない階に空レイヤを作らない（M5 ロフト FL・
  M6 垂木/野地板と同じ方針）。
- ✅ `parse/Context` 拡張: 横架材命令を**コンテキストがキャッシュ**する（`Context::members`）。
  ストーリ（母屋・登り梁レベルの有無）・垂木（桁幅）・登り梁の補正が同じ 1 回の解析結果を
  共有する（要素ごとに組み立て直すと全 `IfcBeam` の走査が 3 回走る）。
- ✅ `core/Document`: `MemberCommand`（`memberId` / 断面 / 天端中央線 / 両端の天端 Z ＋
  `StoryBoundCommand` 2 つ）＋ `Document.members`。`validateDocument` に横架材の検証
  （レイヤ・クラス・構造材 ID が非空／断面が正／天端中央線が非縮退／両端のレベル種別が非空）。
- ✅ `draw/Member`: 構造材ツール（`StructuralMember`）を**パス（天端中央線の 3D ポリライン。
  両端とも同じ Z）＋断面プロファイルのグループ**から `CreateCustomObjectPath` で生成し、
  クラス → プラグインスタイル（`木質構造材_横架材`）→ 両端の `SetObjectStoryBound` →
  個別フィールド（構造材 ID・断面・種別）→ `ResetObject`。全配置後に `UpdateStyledObjects` を
  1 回（`SetPluginObjectStyle` は関連付けまでで描画属性をプッシュしないため。Python 版 #56）。
  **パスに傾斜を持たせない**（バインドの高さ差がパス由来の部材長へ加算され二重適用になる）。
  ISDK には VS の `Move3D` が無いので、パスの頂点そのものを最終位置で作る（M6 と同じ作法。
  Z が絶対かレイヤ相対かは `draw/Member.cpp` の `kPathZ` 1 か所で切り替えられる）。
  スタイル名 → RefNumber は `GetNamedObject` ＋ `GetObjectInternalIndex` で引く
  （ISDK に名前から引く呼び出しが無いため）。
- ✅ テスト: `ParseMemberTests`（Python 版 `test_ifc_member.py` を移植: 配置・断面・材種・
  構造材 ID・天端補正・レイヤ振り分け・バインド・クラス・鉛直軸のスキップ・登り梁の任意断面と
  直切りの幾何・食い込み調整の全ケース・センタリング・決定性・全フィクスチャ）、
  `ParseNoboribariTests`（`test_ifc_noboribari.py` を移植: 屋根面の天端 Z と内包・端部詰め・
  屋根面の選択・スナップ・素通し・フィクスチャからの屋根面収集）。`ParseRafterTests` に桁幅
  参照、`ParseStoryTests` に母屋レベル、`CoreDocumentTests` に横架材の検証、
  `ParseContextTests` に横架材キャッシュのケースを追加。
  **注意: 検証済みフィクスチャに登り梁を含むモデルは無い**（`木梁:登り梁:…` が 1 件も無い）。
  そのため登り梁の経路は合成モデル・合成命令でのみ検証されており、実データでの確認は
  ローカル目視（登り梁のあるモデルを開く）に委ねる。

**ローカル確認（済）:** 各階に土台・梁・桁が正しい高さ・断面・クラスで並び、母屋・棟木・
登り梁が専用レイヤ（`n-母屋` / `n-登り梁`）に分離されることを実機で確認した。OIP の
スパン・長さが実寸（例 2170）、勾配 0°、主幅 105 / 主高さ 120、構造材 ID も命令どおりで、
高さの二重加算も端部の食い込みも無く、登り梁の屋根面スナップも狙いどおりに乗った。

**確認の過程で判明した描画側の要点**（M8 以降も同じ落とし穴を踏まないための記録）:
  * **PIO のパスは 2D ポリライン（`VWPolygon2DObj`）で渡す。** 当初 2 頂点の 3D ポリラインに
    絶対 Z を持たせたところ、構造材は 451 本生成されるのに**長さ 0 で画面に何も描かれなかった**
    （OIP は「スパン 0 / 長さ 0」。PIO がパスを挿入点としてしか読んでいなかった）。GridAxis
    （M1）と構造材（M7）の 2 例で確かめた作法なので、柱以降も 2D ポリラインから始める。
  * Python 版の NURBS 曲線パスは **ISDK では再現できない**（`VWNURBSCurve` は評価専用で制御点
    から構築できず、`CreateNurbsCurve` は 1 点しか作れず頂点追加の呼び出しが無い）。
  * **高さはストーリバウンドだけで決まる**（パスに Z を持たせる必要は無い）。
  * プロファイルグループへは `VWGroupObj::AddObject` で入れ、**空でないことを確かめる**
    （空の断面は「オブジェクトはあるのに描かれない」を招く）。
  * パラメータは名前を解決してから書き、**読み戻して確かめる**（`draw/DrawUtil` の
    `ResolveParamName` / `SetParamRealChecked`）。異常は完了ダイアログの診断行に出る。

**残る未確認事項:** プラグインスタイル `木質構造材_横架材` の**描画属性**（テクスチャ等）が
`UpdateStyledObjects` で反映されているかは、上記の確認では見ていない（スタイルの関連付け
自体は OIP で確認済み）。また**登り梁を含むモデルでの確認は 1 例のみ**で、検証済み
フィクスチャには登り梁が 1 件も無い（`tests/fixtures/README.md`）。

## M8 — 柱（管柱・通し柱・小屋束）★形状へ合わせる支持部材 ✅

**目的:** 柱を構造材ツールで鉛直材として描き、span レイヤに分けて配置する。登り梁の
端部詰め（M7）を柱面まで最終化する。**Python: `ifc/column.py` / `vw/column.py`。**

- ✅ `parse/Column`: `IfcColumn` → `ColumnCommand`。配置座標（`columnPosition2D`）・断面と
  柱高さ（横架材と同じ `memberProfileDims`。XDim=幅／YDim=せい／Depth=柱高さ）・種別
  （`resolveColumnType`）・柱頭/柱脚金物（`IfcMechanicalFastener` を同一平面座標で対応付け、
  型名を**加工せず**仕様にする）・**span（from/to レベル）判定**（`resolveColumnToLevel`。
  上階の横架材**下端**＝M7 の members を参照し、到達階の天端を超える柱は屋根束として
  半整数へ）→ `{from}to{to}-柱` レイヤ、`structuralUse`（柱=4／小屋束=5）、金物を含む
  `memberId`。**小屋束の断面は直上に乗る母屋・棟木・登り梁の幅に合わせた正方形へ置き換える**
  （`memberWidthOnTop`。ホームズ君 IFC の小屋束断面は当てにならないため）。
- ✅ `parse/Story` 拡張: span 柱レベル（`collectColumnLayersByStory`）を各ストーリの
  レベル列**先頭**（FL／軒高の直上）へ (from, to) 昇順で追加。レベル種別はレイヤ名そのもの。
  span レイヤ名の生成・分解（`spanLayerName` / `parseSpanLayer`）はレイヤ名規約と同じ
  `parse/Story` に置く。柱の無い階には作らない（空レイヤを作らない。M5〜M7 と同じ方針）。
- ✅ `parse/Context` 拡張: 柱命令を**コンテキストがキャッシュ**する（`Context::columns`）。
  ストーリ（span レベル）・Document の columns・登り梁の端部詰めが同じ 1 回の解析結果を共有する。
- ✅ `parse/Noboribari` 最終化: 登り梁端部の食い込み解消を柱面まで適用（`noboribariEndTrim` /
  `correctOneNoboribari` / `correctNoboribari` が柱を受け取り、方向を持たない柱は断面の
  軸平行矩形として `noboribariColumnPenetration` で食い込み量を求める）。
- ✅ `core/Document`: `ColumnCommand`（span レイヤ／構造材 ID／構造用途／断面・柱高さ・
  下端 Z／柱頭・柱脚金物／上下端の `StoryBoundCommand`）＋ `Document.columns`。
  `validateDocument` に柱の検証（レイヤ・クラス・構造材 ID・構造用途が非空／断面と柱高さが
  正／上下端のレベル種別が非空）。
- ✅ `draw/Column`: 構造材ツール（`StructuralMember`）で鉛直に生成＋上下端バインド＋
  プラグインスタイル `木質構造材_柱・束`、全配置後に `UpdateStyledObjects` を 1 回（M7 と同じ）。
  パスは下端 → 上端の鉛直な 2 点の NURBS 曲線（下記「柱のパスは…」）。
  断面プロファイルの生成は `draw/DrawUtil` の `CreateRectangleProfileGroup` へ集約し、横架材と
  共有した（矩形の置き方＝断面基準点の規約だけが呼び出し側で異なる）。
- ✅ テスト: `ParseColumnTests`（`test_ifc_column.py` / `test_ifc_column_span.py` の全ケースを
  移植: 配置・種別・構造材 ID・金物の対応付け・to レベル判定・span の列挙とまとめ・上下端
  バインド・構造用途・クラス・小屋束の断面合わせ・センタリング・決定性・全フィクスチャ）。
  `ParseStoryTests` に span レイヤ名の往復と span レベルの積み方、`CoreDocumentTests` に柱の
  検証、`ParseContextTests` に柱キャッシュのケースを追加。

**［描画側の要点］柱のパスは鉛直な 2 点の NURBS 曲線で作る（`Add3DVertex`）。** M7 の横架材は
**2D ポリライン**をパスにする作法で確定したが（`draw/Member.cpp` 冒頭）、柱は鉛直材なので平面へ
落とすと 1 点に潰れ、2D ポリラインでは表せない。Python 版と同じく `CreateNurbsCurve` ＋
**`Add3DVertex`**（VS の `AddVertex3D` にあたる ISDK の呼び出し）で下端 → 上端の 2 点を作る。
M7 のコメントにある「頂点を足す呼び出しが無い」は取りこぼしで、`VWNURBSCurve` が評価専用
（制御点から構築できない）なのは事実だが ISDK 側に `Add3DVertex` がある（ci-debug の
`sdk-grep` で確認）。**M7 で長さ 0 になった `VWPolygon3DObj` のパスは使わない。**
鉛直パスが 2 点になったか・断面が入ったかは**読み戻して数え、完了ダイアログの診断行に出す**
（横架材と同じ枠組み。ただし**スパン＝平面投影長は数えない**——鉛直材では 0 が正常）。

**【高さは「鉛直パス」と「バウンドの差」の両方が要る】** ローカル確認 3 周で切り分けた要点で、
**どちらか一方だけでは描かれない**。

| 周 | 鉛直パス | バウンドの差 | 結果 |
| --- | --- | --- | --- |
| 1 | 1 点（`Insert3DVertex` が効かず） | 0（小屋束） | スパン/長さ/高さ 0。オブジェクトはレイヤ原点に置かれ、バウンドの offset まで VW が「レベル Z − オブジェクト Z」で再計算した値（−5961）に上書きされた |
| 2 | 1 点 | 柱高さ | OIP の高さ・始端/終端オフセットは命令どおり（786.33 / 0 / 786.33）になったが、**長さ 0 で描かれず** |
| 3 | **2 点**（`Add3DVertex`） | 管柱＝柱高さ／小屋束＝0 | **管柱は正しく描かれ、小屋束だけ高さ 0** |

したがって VW 2026 の構造材 PIO では**バウンドの差が高さを支配し**、鉛直パスはその高さで実体を
作るために要る。**Python 版 #116（小屋束の上端 offset を下端と同値にして二重加算を避ける）は
この SDK では当てはまらない**——同値にすると高さ 0 になるので、どの柱でも「バウンドの差＝柱
高さ」になるよう上端 offset を「下端 ＋ 柱高さ」にする（`parse/Column.h` の「Python 版との差異」）。
1 周目の時点で span レイヤ・クラス・断面（`90×90 - 小屋束`＝直上の母屋幅に合わせた補正が効いて
いる）・スタイルは正しく、以降の 2 周は高さの切り分けだけだった。

**ローカル確認（済）:** 柱・小屋束が span レイヤ（`1to2-柱` / `2to3-柱` / `2to3.5-柱` /
`3to3.5-柱` …）に分かれてストーリに紐づき、管柱・通し柱・小屋束がいずれも実体として正しい
高さで立つことを VW 実機で確認した。断面（小屋束は直上の母屋幅に合わせた正方形 `90×90 -
小屋束`）・クラス・構造材 ID（柱頭/柱脚金物の仕様を連結）・プラグインスタイル
`木質構造材_柱・束` の関連付けも命令どおり。**OIP のスパン・長さが 0 なのは鉛直材では正常**
（平面投影長）で、高さは始端／終端高さオフセットの差が担う。

**確認の過程で判明した描画側の要点**（M9 以降も同じ落とし穴を踏まないための記録。上表参照）:
  * **鉛直材のパスは `CreateNurbsCurve` ＋ `Add3DVertex`。** `Add3DVertex` が VS の
    `AddVertex3D` にあたる（`Insert3DVertex` は別物で、頂点が増えない）。M7 で長さ 0 に
    なった `VWPolygon3DObj` のパスは使わない。
  * **高さはパスとバウンドの両方が要る。** バウンドの差が 0 だと（パスが正しくても）高さ 0 に
    なるので、上下端 offset は常に実際の下端／上端 Z までの距離を入れる。
  * **ローカル確認の切り分けは診断行と OIP の読み合わせで進む。** 「鉛直パスが 2 点に
    ならなかった」件数と、OIP の 高さ／始端・終端オフセット／スパン・長さ の組み合わせで、
    原因が解析側（命令の値）か描画側（パス・パラメータ）かを 1 周ごとに絞り込めた。

---

## M9 — 基礎（立上り＝壁・底盤＝スラブ）＋基礎ストーリ 🟨

**目的:** 基礎の主要 2 オブジェクトと基礎ストーリを描く。上部形状（床・屋根・部材）とは
独立している。地中梁・人通口・壁結合・配筋は M10 に分割。
**Python: `ifc/footing.py`（一部）/ `vw/footing.py`。**

- ✅ `parse/Footing`: 基礎ストーリ命令（`基礎`/suffix `F`/GL=0）。立上り（`基礎梁…` の
  `IfcFooting`）→ `WallCommand`（`F-立上り`。壁芯＝配置原点から押し出し方向・壁厚＝矩形断面の
  幅・非矩形断面はスキップ。下端は自階 `GL`、上端は 1 階（上階）`横架材天端` へバインドし、
  offset は実 Z との差。**下端は IFC 実形状のまま**——下記「呑み込みは行わない」）。
  底盤（`…底盤…` の `IfcSlab` / `IfcFooting`）→ `SlabCommand`（`F-底盤`。天端の絶対 Z ＋
  底盤天端レベルへのバインド、Z 厚を整数 mm に丸めたコンクリート厚）。基礎クラス
  （立上り＝`04構造-01基礎-03立ち上がり`／底盤＝`…-02基礎スラブ`）は M4 の枠
  （`parse/StructuralClass`）へ定義した。
- ✅ **［Python 版と異なる点・ローカル確認で決定］立上り下端の呑み込みは行わない。**
  Python 版は下端を `_SLAB_BITE`＝10mm **下げて**「底盤に呑み込ませる」が、ホームズ君は
  基礎梁を**底盤の底面まで**の全高でモデリングしている（実測: 伏図次郎・サンプル1 は全本が
  Z=−100＝底盤天端 50 − 底盤厚 150、スキップフロアは −100 と −150 が混在）。下端は既に
  底盤の底面と一致しているので、下げても**底盤の下へ 10mm 突き出すだけ**で意図と逆になり、
  実機で −110 という中途半端な位置になっていた。そこで**ソリッドの下端をそのまま使う**
  （＝「基礎梁全体を立上り壁としてモデリング」する規約。GL−100〜−250 の慣例に収まる）。
  IFC が持つ深さの差（スキップフロアの −150）もそのまま残る。**内外の深さの違いは
  `地中梁`（M10）が持つ**ので、基礎梁側で作り込まない。
- ✅ 立上りの後処理 2 段: `mergeWallCommands`（同一直線・同一断面のものだけ 1 本へ。隙間・
  側並び・断面違いは統合しない）→ `extendFreeWallEnds`（他の立上りと交差しない端点を
  **柱芯 + 半壁厚**へ延長。半島状の自由端が土台の半材せいぶん長くなるのを防ぐため、M8 の
  柱命令から終端柱を探して基準点を柱芯へ寄せる）。実データで 39 本 → 17 本（伏図次郎）。
- ✅ 底盤の後処理 2 段: `mergeSlabCommands`（同厚・同高で連続する底盤を**任意向きの単純
  多角形の和**で 1 枚へ。傾いた底盤・45 度取合いの斜め辺も統合でき、隙間・角接触・穴のできる
  成分＝布基礎の升目状ラティスは統合しない）→ `alignSlabsToWallFaces`（外形は立上りの
  **壁心**にあるので、辺ごとに沿う立上りの**外面**＝壁心 + 半壁厚まで外側へ広げる。実データで
  外周が 60mm＝120mm 壁の半壁厚ぶん外へ動くことを確認）。
- ✅ `parse/Context` 拡張: 立上りの命令を**コンテキストがキャッシュ**する（`Context::walls`）。
  Document の walls と底盤の外面合わせが同じ 1 回の解析結果を共有する（立上りの自由端は
  柱芯へ寄せるので、この計算は `columns` を入力に取る）。
- ✅ `parse/BuildDocument`: 基礎要素があれば**基礎ストーリを stories の先頭（最下層）へ**
  置く（Python 版 `build_document` と同じ）。Elevation=0 で最下層になり、レイヤの希望
  スタック順（最上階→最下階）でも最下段に積まれる。
- ✅ `core/Document`: `WallCommand`（壁芯・壁厚・上下端の `StoryBoundCommand`）＋
  `SlabCommand`（外形・スタイル名・構成層・コンクリート厚・天端 Z ＋バインド）＋
  `Document.walls` / `.slabs`。`validateDocument` に立上りの検証（レイヤ・クラス・壁厚が正・
  壁芯が非縮退・上下端のレベル種別が非空）と底盤の検証（床板と同じ関門＋コンクリート厚が正）。
- ✅ `draw/Footing`: 立上りは `CreateWall`(始点, 終点, 壁厚) →クラス・by-class 属性→
  **`SetWallOverallHeights`** で上下端をストーリレベルへ→壁スタイル `基礎 - 木造ベタ基礎150mm`
  → `ResetObject`。底盤は M5 床板と同じ作法（`CreateSlab`→クラス→スラブスタイル→
  `SetSlabHeight`（**天端の絶対 Z**）→`SetObjectStoryBound`→`ResetObject`）。作れない場合は
  それぞれ壁芯の直線／外形ポリゴンへフォールバックする。
- ✅ **［Python 版と実現手段が異なる点］`DoubLines`→`Wall` の 2 手順は要らない**——ISDK の
  `CreateWall` は壁厚を引数に取る（ci-debug の `sdk-grep` で実在を確認）。高さは**壁専用の
  `SetWallOverallHeights`**（`SStoryObjectData` 2 つ）で与える: 汎用の `SetObjectStoryBound`
  では壁の高さ基準が確定せず、デザインレイヤの「壁の高さ」設定に従ってしまう（構造材・
  スラブでは効くが壁は専用関数が要る。Python 版 CLAUDE.md「基礎」節と同じ知見）。
- ✅ **［Python 版と異なる点・意図的］スラブスタイルは列挙・複製ではなく名前で用意する。**
  Python 版は `BuildResourceList` で既存スタイルを列挙し、既定 150mm スタイルを複製して
  コンクリート層の厚みだけ差し替える。本移植は M5 床板と同じく「名前で引き、無ければ
  `CreateSlabStyle` で作り、構成層を命令どおりに組み直す」（スタイル名・構成層は解析側が
  命令に載せる）。**この手順は `draw/DrawUtil` に 1 つだけ置き、床板と底盤で共有する**。
- ✅ **［Python 版と異なる点・意図的］基礎ストーリのレベルは 2 つだけ。** Python 版は
  基礎天端（アンカーボルト）→ GL（立上り）→ 床束 → 底盤天端 の 4 レベルを常に持たせるが、
  本移植は本 M で描く 2 つ（GL・底盤天端）だけを作る。アンカーボルト・床束は M11 の要素で、
  いま作ると空レイヤが残るだけになる（M5 ロフト FL・M6 垂木/野地板・M7 母屋・M8 span 柱と
  同じ「描画対象の無いレベルは先に作らない」方針）。M11 で 基礎天端 を GL の上段へ、床束 を
  底盤天端 の上段へ挿し込めば Python 版と同じ 4 レベルのスタックになる。
- ✅ **［Python 版と異なる点・当面の差］統合キーに配筋を含めない。** Python 版は立上り・
  底盤の統合キーに配筋を含め、配筋の違う部材を 1 つに潰さない。本 M は配筋（M10）を持たない
  ため含めていない。結果として**配筋だけが違う底盤は 1 枚に統合される**（伏図次郎: Python 版
  3 枚 → 本移植 2 枚）。**M10 で配筋を足すときはキーにも足す**（`parse/Footing.cpp` の
  `wallSectionKey` / `slabMergeKey` にその旨を明記してある）。
- ✅ テスト: `ParseFootingTests`（`test_ifc_footing.py` の M9 相当ケースを移植: 立上りの統合
  （接触・重なり・3 連鎖／隙間・側並び・直交・断面違い・高さ違いは非統合）、自由端の延長
  （孤立・軸方向・L コーナー・T 字・長さ 0・別レイヤ・柱芯スナップ・遠い柱／軸外の柱を使わない）、
  底盤の統合（隣接矩形・L 字 6 頂点・隙間・角接触・厚み違い・高さ違い・穴のある輪・斜め辺・
  回転グリッド・離れた群）、外面合わせ（4 辺・沿う壁が無い・辺ごとの厚み）、実フィクスチャの形
  （レイヤ・クラス・バインド・`elevation = 底盤天端 + offset` の不変条件・呑み込み −110mm・
  外面 60mm・全フィクスチャ・決定性））。`CoreDocumentTests` に立上り・底盤の検証、
  `ParseContextTests` に立上りキャッシュのケースを追加。

**ローカル確認（未）:** 以下を VectorWorks 実機で確認する。

- `基礎` ストーリができ、`F-立上り`（GL）・`F-底盤`（底盤天端）レイヤが**最下層**に並ぶ。
- 立上りが**壁厚ごとの壁スタイル**（`基礎立上り - コンクリート 120mm` 等）で描かれ、
  テンプレートの既存スタイルが書き換わっていないこと。
- 立上りが壁オブジェクトで描かれ、**高さが命令どおり**（下端＝IFC の基礎梁ソリッド下端。
  伏図次郎なら GL−100＝底盤の底面、スキップフロアは −100 と −150 が混在。上端が 1 階の
  横架材天端）。デザインレイヤの「壁の高さ」設定に引きずられていないこと
  （`SetWallOverallHeights` が効いているか）。下端が底盤の底面と面一（coplanar）になるので、
  断面ビューポートで境界線が不安定に出ないかも併せて見る。
- **壁厚が命令どおり**（120 / 150 / 300mm が混在する）。壁スタイルは壁厚ごとに作るので、
  スタイルを当てても厚みは保たれるはず。
- 連続する立上りが**1 本に統合**され、通り芯の交点で細切れになっていない。自由端が
  柱芯から半壁厚だけ外に出ている（土台の半材せいぶん長くなっていない）。
- 底盤がスラブで描かれ、**天端が底盤天端**（伏図次郎なら Z=50）に来る。コンクリート厚ごとの
  スラブスタイル（`基礎スラブ - コンクリート 150mm / 捨てコン 30mm / 砕石 100mm` 等）が
  でき、構成が 上から コンクリート → 捨てコン → 砕石 になっている。**テンプレートに同名の
  スタイルがある場合、それが書き換わっていない**こと（インポートは別名 `… (2)` を作る）。
- 連続する底盤が**1 枚に統合**され、外周が立上りの**外面**（壁心 + 半壁厚）まで出ている。
  布基礎（升目状ラティス）のモデル（`グレー本モデルプラン1【3階】`）では統合されず元のまま
  28 枚残る（部屋の下までコンクリートで埋まっていないこと）。
- 立上りと底盤・上部構造（土台・柱）の取り合いに隙間や食い違いが無い。

## M10 — 基礎の高度化（地中梁・人通口・壁結合／配筋は保留） 🟨

**目的:** 基礎の残り機能。M9 の上に積む。**Python: `ifc/footing.py` / `ifc/reinforcement.py`。**

- ✅ 人通口（`parse/Footing`）: 立上りソリッドから**天端の下方へ削り取った**別ソリッド
  （差演算＝`IfcBooleanResult` の DIFFERENCE の**第 2 オペランド**）を抽出し
  （`collectWallOpenings`。抽出そのものは `parse/IfcGeometry` の `elementVoidSolids`）、
  **統合・自由端延長の済んだ**立上りへ当てはめて分割／切り下げる（`applyWallOpenings`）。
  開口下端が**底盤天端以下**なら区間を空けて両側だけを、それより高ければその区間だけ天端を
  開口下端へ切り下げる。開口境界の端は**実寸法のまま**（人通口は実寸法でモデル化されている
  ので長さ補正しない）。**端部が他材で削られた全高の差演算を人通口と誤認しない**よう、
  「天端まで届き底面には届かない」削りだけを拾う。実データ: サンプル1 で 9 か所、
  スキップフロアで 16 か所。
- ✅ 壁結合（`parse/Footing` → `draw/Footing`）: 交差する立上りを**同一交点ごとの
  ジャンクション**にまとめ（`buildWallJoinCommands`）、通し壁があれば最も高い通し壁を
  バックボーンに X／T、無ければ天端降順ではじめの 2 本を L・以降を T にする。ただし
  **十字は縦横 2 本の壁のまま**にして X（交差結合）で繋ぐ（分割はしない。下記）。天端の違う
  立上りどうしは低いほうを `a` にして `capped=true`。ピック点は**交点そのものではなく
  「残す側」へ寄せた点**（交点は相手壁芯上にあり残す側が曖昧で、VW が L でコーナーを詰めない。
  Python 版 #84）。描画は `JoinWalls`（ISDK にあることを ci-debug で確認）で、**壁ハンドルは
  命令インデックス → ハンドルの対応表**（`draw::WallHandles`）で受け渡す（Document に
  ハンドルを載せない。CLAUDE.md「所有権」）。`showAlerts=false` で結合失敗のダイアログを抑止。
- ✅ 地中梁（`parse/Footing` → `draw/Footing`）: 台形断面の水平押し出しを
  **`core::ModifierCommand`（断面 (u,v)・押し出し長・断面原点のワールド座標・方位角）**へ
  取り直し（`buildGroundBeamModifiers`）、同一軸線・同一断面・同一高さで連続するものを
  1 本へ統合し（`mergeGroundBeamModifiers`）、平面外形が最も重なる底盤の `modifiers` へ
  振り分ける（`attachGroundBeamModifiers`。どの底盤にも入らないものは重心が最も近い底盤へ
  フォールバックし取りこぼさない）。**単独のスラブ命令にはしない**。描画は台形プリズムを
  **2 回**作る: (1) 削り取りモディファイア群を `SetCustomObjectProfileGroup` で通常スラブへ
  渡して底盤を clip、(2) 同じプリズムを可視の 3D ソリッドとして同じレイヤ・クラスで置く
  （こちらだけ天端を 10mm 底盤へ呑み込ませる＝`core::raiseModifierTop`）。実データ:
  サンプル1 で 17 本、伏図次郎で 13 本（統合後）。
- ❌ **［調査済み・不可］`ModifySlab` で「足す」噛み合わせはできない**（PR #44。**この API を
  もう一度試さないこと**）。2 回作るのは Python 版（VectorScript）の回避策なので、C++ SDK なら
  1 回で噛み合わせられるのではないか、を実際に試した結果:

  * SDK には**専用の API がある**——`ISDK::ModifySlab(slab, modifier, isClipObject,
    componentFlags)`（`Interfaces/VectorWorks/ISDK.h`。`vs.py` の説明は "Adds to or clips
    from a slab."、`isClipObject` は "Whether the modifier is an add object or a clip
    object"）。**API の形の上では `isClipObject = false` が「足す＝噛み合わせる」にあたる。**
  * しかし**実機（VW 2026）では VectorScript 版と同じく失敗する**。`ModifySlab` は
    **「選択が間違っています」のダイアログを出して false を返し**、底盤は直方体のまま・
    地中梁は独立したプリズムのまま残る。ダイアログは**呼んだ回数ぶん**出る。
  * この文言は VW のコマンドが**ドキュメントの選択**を検証したときの定型なので、
    「全解除 → 底盤とプリズムだけを選択 → 呼ぶ」も試したが**結果は同じ**だった。
    引数のハンドルの問題でも選択状態の問題でもなく、**VW 側の不具合の可能性が高い**。
  * SDK に他の手段は無い（`sdk-grep` で確認: スラブのモディファイアを扱う API は
    `ModifySlab` だけで、`Interfaces/` にスラブ用のインターフェースも、モディファイアの
    add/clip を表すオブジェクト変数・レコードも無い）。
  * したがって**削り取り＋可視ソリッドの 2 回作りが現時点の最善**で、上の実装を維持する。
- 🔍 **［UI 産物の解析結果］add と clip を分けているのは「押し出しに付く補助データ
  オブジェクト」**（VS スクリプトで UI の結果をダンプして判明。ここまで分かっているので、
  再開するならこの続きから）。
  * UI の「3D オブジェクトとスラブを噛み合わせる」ダイアログは **構成要素のポップアップ**と
    **「構成要素の境界で 3D オブジェクトをクリップする」チェックボックス**の 2 つを持ち、
    これが `ModifySlab` の `componentFlags` / `isClipObject` に対応するとみられる
    （＝`isClipObject` は「add か clip か」ではなく**クリップするかどうかの option**。
    SDK ドキュメントの "The adding or clipping object" は実態と食い違っている）。
  * **構造は我々の削り取りと同じ**——スラブ（`type=86`）のプロファイル群（`type=11`）に
    押し出し（`type=24`）が入るだけで、クラスは `一般`・レコード無し・モディファイア専用
    クラスも作られない。
  * **違いは押し出しに付く補助オブジェクト**。dev ビルドへ一時的に足した診断コマンド
    （型番号＋`GetDataTag` を書き出す。コミット `ce3227f` の
    `src/Extensions/ExtMenuDumpAux.*`。用が済んだので削除済み）で、**インポートした底盤に
    UI で 1 本だけ噛み合わせた状態**——同じスラブに我々の削り取り 12 本と UI の噛み合わせ
    1 本が同居する——を覗いて確定した:

    | | 我々の削り取り | UI の噛み合わせ |
    | --- | --- | --- |
    | 押し出しの aux | `type=114` ＋ `DMDT`×3 | `type=114` ＋ **`type=121`** ＋ **`OPhI`** ＋ `DMDT`×4 |

    ※ `type=114` は**両方に付く**（VectorScript でのダンプから「UI 版だけに付く」と読んだのは
    別文書どうしを比べた誤り）。**判別情報は `type=121` の補助オブジェクトと `OPhI` タグの
    データオブジェクト**で、しかも**スラブ本体にも同じ組が付く**（両側にリンクが張られる）。
    オブジェクト変数の総なめで出た差（605 / 606 / 621 / 622）は `ovWallStartHeightBottom`
    等の**壁の高さ（座標）**を Boolean/Int として読んだノイズで、判別情報ではない。
  * **この 2 つは SDK からは作れない**。`NewDataObject(attachTo, tag, size)` が作れるのは
    データオブジェクト（`type=76`）だけで、`type=121` のノードを作る API は無い。`OPhI` /
    `DMDT` / `OSbn` / `wesl` といったタグは**SDK ヘッダのどこにも定義が無い**（VW 内部専用）
    ため、中身のバイト列も分からない（タグ付きデータは (コンテナ OSType, 型, タグ ID) を
    知らないと読めず、列挙もできない）。
  * **したがって噛み合わせは現時点の SDK では実現できない**。UI でできるのに API で
    できないのは VW 側の不具合か未公開機能で、**Nemetschek へ報告して `ModifySlab` の修正を
    待つのが筋**。描画結果は現行の 2 回作りで足りているので、この道はここで打ち切る。
- ✅ **［ローカル確認で判明・Python 版に無い］端部のキャップは解析側が端ごとに決める。**
  VW の壁は端部を閉じる線（キャップ）を**壁ごと**に持ち、明示しなければドキュメントの壁ツール
  設定に従う。結合（`JoinWalls` の `capped`）任せにしたところ、実機で **T 字でぶつかる側の
  端線が見えたまま**・**自由端に閉じ線が無い**という取り違えが両方起きた。そこで
  `applyWallCaps` が壁結合命令から端ごとに「閉じるか」を決めて `WallCommand.capStart` /
  `capEnd` に載せ、描画側が `SetWallCaps` で設定する（**結合の後にも入れ直す**——`JoinWalls` が
  結合した端のキャップを書き換えるため）。規則は「その端に**閉じない結合**（同じ天端の相手と
  一体になる結合）が 1 つでもあれば開ける、無ければ閉じる」で、自由端＝閉じる・同じ天端との
  L / T / X＝開く・天端の違う相手とだけ取り合う端＝閉じる、が自然に出る。
- ✅ **［ローカル確認で判明・M9 の自由端延長の不具合］同一直線上の突き合わせを自由端と
  見なさない。** 交点判定（`wallIntersection`）は平行な立上りを除外するため、**同じ通りで
  端どうしが接する立上り**はどちらの端も自由端に見え、双方が半壁厚ずつ延びて重なっていた
  （実データで 150mm＝半壁厚 2 つぶんの重なりが 3 組）。`collinearAbutment` で「同一直線上に
  あり区間が接する／重なる隣」を見て、その端は延長しない。**統合できない隣**——上端／下端の
  違う立上り——との突き合わせで顕在化する（統合できる隣は `mergeWallCommands` が 1 本にする）。
  なお実データには**元から半壁厚だけ重なって入力された**立上り（浅い側が深い側へ食い込む）も
  あるが、これは実形状なので動かさず、**天端が同じなら端部を閉じない**（上記のキャップ）ことで
  平面では 1 本に見えるようにした。
- ✅ **［診断］残すのは「VW に結合を拒否された件数」だけ**（`drawWallJoins` の `outNote`。
  `draw/Member` と同じ流儀）。「命令はあるのに繋がらない」ときに解析側の判定（種別・ピック点）
  を疑う手掛かりになる。実データでは 0 件なので、出たら異常。
- ❌ **［切り分け用に足して外した診断］レイヤ上の壁を数える／命令と端点を突き合わせる。**
  M10 の切り分けでは「レイヤの壁の絶対本数」「命令に無い壁の座標」「結合後に増えた本数」を
  完了ダイアログへ出していた（これで X 結合の仕様と地中梁の平行移動を特定できた）。原因が
  分かった時点で役目を終えたので**外した**——**結合は壁芯の端点を動かしうるし、X 結合は仕様で
  壁を 1 本増やす**ので、本数や端点の一致を「異常」として出すと平常時から鳴り続ける。
  同じ切り分けが再び必要になったら、`GetObjectBounds` と `VWWallObj::GetPoints` で同じものを
  作り直せる（当時の実装は PR #41 の履歴にある）。
- ✅ **［ローカル確認で判明］結合の後に壁をリセットする。** `JoinWalls` は 54 件中 53 件を
  受け付けているのに、平面では T 字の取り合いの面線が消えなかった。切断高さ（レイヤの
  カットプレーン）・壁スタイルのコア構成要素（`SetCoreWallComponent`）はいずれも実機で
  **外れ**で、効いたのは **`ResetObject`**——結合は成立していて 2D 表現だけが古いまま
  だった（手で壁スタイルを編集すると表現が変わる、という実機の手掛かりが決め手）。
  順序は キャップ → リセット（`drawWallJoins` の末尾）。
- ✅ **［ローカル確認で判明・M9 から潜んでいた不具合］同一直線の 2 本が集まる交点は、
  深いほうを通し壁にする。** 上端が同じで**下端だけ違う**立上り（底盤厚が違う箇所で起きる。
  下端は統合キーに入るので 1 本にできない）が、直交する立上りの位置で突き合わさると、
  **通し壁が 1 本も無い交点**になる。「天端降順の先頭 2 本を L」規則はこの同一直線の 2 本を
  選んでしまい、コーナーにならないので VW が拒否する——実データの拒否 1 件 (6370,1820)。
  対処は 2 段:
  - `extendDeeperCollinearEnds`（`buildWallCommands` の統合・自由端延長の後）で、
    **深いほう（下端が低い。同値なら添字が小さいほう）を直交する立上りの半壁厚だけ伸ばす**。
    伸ばす量は自由端延長と同じ考え方（相手の外面まで）で、これで交点を越えて通し壁になる。
  - ジャンクションに**同一直線の 2 本がある**なら、そこにコーナーは無いものとして、深いほうを
    通し壁に選び直交する立上りをそこへ T 結合する（同一直線の隣とは結合せず、端部のキャップで
    1 本に見せる）。実データでは (6370,1820) が `T a=[49] b=[06]` の 1 件になる。

  より正確なのは「同一直線の立上りを 1 本に統合して下端の違いを切り欠きで表す」ことだが、
  VW の壁は下端の切り欠きを構成層でも高さバインドでも表せず別途ソリッドで削る仕組みが要り、
  変更にも弱くなるため採らない。
- ✅ **［診断］拒否された結合を交点の座標で名指しする**（`drawWallJoins` の `outNote`）。
  上の同一直線 L 結合の不具合はこれで場所が分かって特定できた。
- ❌ **［測ってみて外した診断］「結合の前後で `a` の壁の壁芯端点が動いたか」は結合が効いたか
  を測れない。** 実機では 55 件中 48 件が「動かなかった」のに T 字の取り合いは正しく描かれて
  いた——**VW は結合を記録しても壁芯の端点は変えず、表示のときに取り合いを解決する**。
  同じ測り方を足し直さないこと（`drawWallJoins` にコメントで残してある）。
- ✅ **［ローカル確認で判明・VW の仕様］X 結合（交差結合）は a の壁を交点で 2 本に分割する。**
  VW ヘルプ「X wall joins」に「両壁の長さは変わらない（＝すでに交差している必要がある）」
  「**1 本目に選んだ壁が 2 本の壁に分割され、load bearing wall（2 本目）へ結合される**」と
  ある。実データで壁が 50 → 52 本（X:2）へ増えるのはこの仕様どおりで、**バグではない**。
  ここから 2 つの不具合が出ていた（実機の症状「全長の壁は X 結合されず、半分の壁が T 結合
  されている」が両方で説明できる）。
  - **分割されるのは a**（残るのは b）なので、**バックボーンを b にする**。`makeLX` は
    同じ天端なら root（バックボーン）を a にしていたため、通し壁が分割されていた。
    X だけは常に `a=other` / `b=root` にする（root は最も高いので、天端が違うときの
    「低いほうを a」も同時に満たす）。
  - **X 結合はすべて最後に実行する**（`buildWallJoinCommands` の末尾で安定ソート）。
    分割された壁の**ハンドルが古くなる**ので、X より後にその壁を使う結合が残っていると
    分割後の片方だけを相手にしてしまう。実データではまさにこれで、交差する横の立上りは
    両端に T 結合を持ち、X（交点）→ T（端）の順に実行されていた。X を最後に回せば、
    分割の時点で他の結合はすべて全長の壁に対して済んでいる。
- 🟨 **［未検証］同じ通し壁の同じ交点に 2 本目の stem が取り付くときは `kAutoWallJoin`。**
  明示的な T 結合では **`JoinWalls` が両方 true を返すのに先に実行した 1 件だけが図面に
  効いた**（拒否件数は増えない）。通し壁側のピック点を反対側へ寄せて区別させる案は実機で
  描画が変わらず外れたので、2 本目以降は `kAutoWallJoin`（=4。ピック点を無視して VW に種別を
  判断させる）で通す（`core::WallJoinType::Auto`）。1 本目は従来どおり T なので、**交点に
  stem が 1 本だけの既存の T 結合（実機で正しく結合されている）の引数は変わらない**。
  実データで Auto になるのは 1 か所。**効果は未確認**。
- ✅ **［ローカル確認で判明・M9 から潜んでいた不具合］同一直線上の 2 本に結合を出さない。**
  平行な立上りは `wallIntersection` が交点を作らないので普段は候補にならないが、**第 3 の壁が
  作った交点**には同じジャンクションとして入ってくる（一直線に並ぶ 2 本の突き合わせ位置を
  別の立上りが横切る形）。そこへ「天端降順の先頭 2 本を L 結合」規則が当たると、コーナーに
  ならないので VW が拒否する——実データの「壁結合: 1 件を VW が拒否しました: (6370,1820)」の
  正体がこれだった（下の診断が座標を出して分かった）。`pushJoin` で同一直線上の組
  （`wallsOnSameLine`）を落とす。同一直線上の突き合わせは結合ではなく端部のキャップ
  （`applyWallCaps` の `collinearAbutment`）で 1 本に見せる。
- ✅ **［ローカル確認で判明］地中梁のプリズムは置いた位置を実測して寄せ直す。** 実機で
  「地中梁の角が底盤の角と合わない」「全体が横は右・縦は下にずれる」と出ていた原因は、
  **描画時の平行移動**だった。実測で確定: 命令の `[00]` は `(-7810,5535)→(4095,5535)`・
  長さ 11905 に対し、図面は `-7760 → 4145`・長さ 11905——**長さは完全一致で、位置だけ軸方向へ
  +50mm**。50mm は F-底盤 レイヤの高さ（底盤天端 Z=50）と一致する。
  `VWExtrudeObj` は押し出しを内部で「2D 基面 ＋ 基準高さ（`baseElevation`）＋ 厚み」に
  持ち替えるため（ci-debug で `VWExtrudeObj.h` を確認）、3D ポリゴンから作ると基面の平面を
  導出したうえで**配置先レイヤの高さを法線方向へ足す**。普通の押し出しは鉛直なのでこれは
  「レイヤぶん持ち上げる」正しい動作だが、地中梁の押し出しは**水平**なのでそのまま軸方向の
  横ずれになる。
  対処は `CreateModifierPrism` の末尾で `GetObjectBounds` を読み、**平面外形の中心が命令どおりの
  位置に来るよう `MoveObject3D` で寄せ直す**。原因の値（レイヤ高さ）を当てにいかず実測で
  補正するので、VW 側の挙動が変わってもこのままで正しい。ずれが無ければ何もしない。
- ❌ **［回り道の記録］「角が合わない」に端部の延長で対処しようとして 3 回外した。**
  原因は上の平行移動で、**伸ばす必要のないところに延長を足していた**。外した規則は
  ① 底盤の外形へレイを飛ばして縁まで伸ばす（`df59f66`）② ①＋「別の地中梁が続いている端は
  伸ばさない」（`8bdf730`）③ 端が直交する立上りの壁芯上にあるときだけ半壁厚伸ばす
  （`4b33b7c`）。**症状が同じまま 3 リビジョン続いたら、対処の方向ではなく前提（＝命令と
  図面のどちらがずれているか）を実測で確かめる**こと——今回は「梁の両端の座標と長さ」を
  1 本もらった時点で確定した。
- ✅ **［IFC 実データで確認］地中梁の断面は立上りの外面に片側の垂直面が付き、内側へ広がる。**
  実機で「梁全体が横は右・縦は下にずれている」ように見えたので生の IFC を確認したところ、
  `IfcExtrudedAreaSolid` の断面ポリラインが `(0,0) (-200,0) (-380,150) (0,150)`、配置原点が
  立上りの外面という形だった（下端 200・天端 380 の逆台形＝ハンチで、天端が底盤の底面に付く）。
  **本移植の描画はこの実形状どおり**で、平行移動のずれは無い。壁芯に対して中心を合わせたい
  場合は IFC の実形状から意図的に変える話になる（現状は変えていない）。
- ✅ **［Python 版と実現手段が異なる点］プリズムは `VWExtrudeObj`（3D 基面＋厚み）で作る。**
  Python 版は「XY 平面に断面を描く → `BeginXtrd` で鉛直に押し出す → `Rotate3D` を 2 回 →
  `Move3D`」という手順で、回転規約が命令の値と対になっていた。ISDK には**ワールド 3D の
  基面ポリゴンを厚みぶん押し出す**コンストラクタがあるので、断面を直接ワールド座標へ写して
  渡す（回転・移動の手順が消え、位置・向き・形状の自由度が「押し出しの向き」1 つだけになる）。
  押し出しは基面の法線方向へ伸びるため、**頂点の並びを法線が梁の走る向きを指すよう揃える**
  （Newell 法。ローカル確認の要点はこの向き）。
- ✅ **［Python 版と異なる点・意図的］可視ソリッドにマテリアルを設定しない。** Python 版は
  文書に登録済みの `基礎コンクリート MT` を名前で引いて割り当てるが、本移植は既存リソースに
  依存しない（スタイルを名前で新規作成するのと同じ方針。M9）。見え方はクラスの by-class 属性。
- ✅ `core/Document`: `WallJoinCommand`＋`WallJoinType`（値は SDK の `JoinModifierType` と一致）・
  `Document.wallJoins`、`ModifierCommand`＋`SlabCommand.modifiers`、描画側から切り離した純計算
  `raiseModifierTop`。`validateDocument` に壁結合（2 本が異なり `walls` の範囲内）と地中梁
  （断面 3 点以上・押し出し長が正）の検証。
- ✅ テスト: `ParseFootingTests`（人通口の分割・切り下げ・端の扱い・非該当開口・複数開口／
  壁結合の L・T・X・`capped`・ピック点・平行は対象外・3 本コーナー／地中梁の統合と非統合・
  掃引外形・底盤への振り分け・フォールバック／実フィクスチャでの不変条件と決定性）、
  `CoreDocumentTests`（壁結合・地中梁の検証規則、`raiseModifierTop` の斜辺追従）。
- ⬜ **配筋（保留）**: レコード `配筋` を立上り・底盤に設定（`Pset_Reinforcement`、鋼種接頭辞
  除去、短辺/長辺→X/Y）。**配筋を足したら統合キーにも足す**（`parse/Footing.cpp` の
  `wallSectionKey` / `slabMergeKey`）——配筋の違う部材を 1 つに統合すると片方の配筋が失われる。
  現状は配筋を持たないため、配筋だけが違う底盤は 1 枚に統合されている（M9 の節）。
  Python: `ifc/reinforcement.py` / `test_ifc_reinforcement.py`。

**ローカル確認（済）:** 以下を VectorWorks 実機で確認した（PR #41。人通口・壁結合・地中梁の
すべてが命令どおりに描かれることを確認済み）。**次に同じ箇所を触るときの見どころ**として残す。

- **人通口**の位置で立上りが切れている（開口下端が底盤天端以下）か、その区間だけ**天端が
  切り下がって**いる（開口下端が高い場合）。開口の端が長さ補正されず実寸法のままであること。
- **コーナー（L）・T 字がきれいに結合**し、立上りが相手壁の外面まで伸びたまま残って
  いないこと（ピック点の寄せが効いているか）。天端の違う立上りの取り合いで、低いほうの
  端部が閉じている（`capped`）こと。結合失敗のダイアログがインポート中に出ないこと。
- **十字（交差）がきれいに繋がっていること。** 交差する立上りは X（交差結合）1 件で繋ぐ。
  **VW は a の壁を交点で 2 本に分割する仕様**なので、完了ダイアログの「壁結合: 結合後に
  立上りが N 本増えました（X:…）」は**出て正常**（実データでは X:2 = 2 本増）。見るべきは
  **重複が無いこと**と、**交差する両方の壁が結合されていること**（バックボーン側の通し壁は
  分割されず全長のまま、交差する側が交点で分かれて両側とも繋がる）。
- 完了ダイアログの拒否件数が **0 件**であること。実データで唯一の拒否だった (6370,1820) は、
  深いほうの立上りを伸ばして通し壁にしたので消えるはず（その交点は T 結合 1 件になる）。
- **下端の違う立上りが一直線に並ぶ箇所**（底盤厚が変わるところ）で、深いほうが直交する
  立上りを越えて通っていて、平面では 1 本の線に見えること。
- **端部のキャップ**（1 周目で取り違えが判明した項目）: T 字でぶつかる側の**端線が消えて**いて、
  何とも取り合わない**自由端には閉じ線がある**こと。**片方の端だけ自由端**の立上りで閉じ方が
  左右逆になっていないか（`SetWallCaps` の leftCap / rightCap を始点／終点と解釈している。
  逆なら `draw/Footing.cpp` の `SetWallCaps` で入れ替える）。
- **同じ通りに並ぶ立上りが重なっていない**こと（半壁厚ぶんの二重線が出ない）。元データから
  重なっている組（浅い側が深い側へ食い込む入力）は、天端が同じなら端部が開いて 1 本に見える。
- 完了ダイアログに「壁結合: N 件を VW が拒否しました（種別ごとの内訳）」が**出ないこと**
  （実データでは 0 件）。出る場合は結合種別・ピック点（解析側）を疑う。
- **地中梁が正しい向き・位置に伸びている**こと（`VWExtrudeObj` の押し出しが基面の法線方向か。
  逆向きなら梁が軸の反対側へ出るので、`CreateModifierPrism` の巻き直しを反転する）。
- 地中梁の位置で**底盤が削り取られ**（断面ビューポートで躯体・捨てコン・砕石の層が地中梁の
  位置に写り込まない）、そこを可視ソリッドが埋めて一体に見えること。断面で底盤と地中梁が
  **構造用図形としてマージ**表示されること（オブジェクト変数 702）。
- 地中梁の天端と底盤の底面の境界線が断面で不安定に出ないこと（呑み込み 10mm が効いているか）。
- **地中梁が命令どおりの位置に置かれている**こと（平行移動の補正が効いているか）。ずれが
  疑われるときは、その梁の**両端の座標と長さ**を測って命令セットと突き合わせる——長さが
  一致して位置だけ違えば描画側の配置、長さも違えば解析側の問題と切り分けられる。
- 完了ダイアログの「立上り N 本（結合 M 箇所）」が命令数どおりであること（`0/M` なら壁
  ハンドルの受け渡しか `JoinWalls` の引数を疑う）。

---

## M11 — シンボル置換系（アンカーボルト・床束・火打・仕口） ✅

**目的:** ハイブリッドシンボルへの置換 4 種。互いに独立で小さく、まとめて 1 マイルストーン。
アンカーボルト・床束は基礎（M9）に、火打・仕口は横架材（M7）・柱（M8）に依存する。
**Python: `ifc/{anchor_bolt,floor_post,fire_brace,joint}.py` / 対応 `vw/`。**

- ✅ アンカーボルト（`parse/AnchorBolt`）: `IfcMechanicalFastener` の**ボルト本体だけ**を採り
  （座金は同じ軸芯にあり二重になる）、型名が `座金なし` を含むかで `アンカーボルト_M16` /
  `アンカーボルト_M12` へ振り分ける。配置先は `F-アンカーボルト`。型名の取得は柱頭・柱脚金物と
  同じ経路なので `parse/Column` の `fastenerTypeName` を公開して共有した（Python 版
  `ifc/anchor_bolt.py` が `ifc/column.py` から import しているのと同じ形）。
- ✅ 床束（`parse/FloorPost`）: ホームズ君 IFC に床束は無いので大引の下へ 910mm 間隔で
  決め打ち配置する。同一直線上で継手（すき間 ≤ 半モジュール）に分断された大引を 1 連へ統合し、
  端部は実部材端ではなく**受けている支持材（土台・他の大引）の芯**にする。配置先は `F-床束`。
  基礎が無いモデルでは空（`parse/Footing` の `hasFoundation`）。
- ✅ 火打（`parse/FireBrace`）: `火打:…` の鉛直押し出し材の footprint から、局所 v の符号反転で
  2 つの端面を拾い、その延長交点を基準点（内角）に、内角の二等分方向＋45 度をシンボルの
  回転角にする。配置先は横架材と同じレイヤ（一般階＝横架材天端、最上階＝軒高）。
- ✅ 仕口（`parse/Joint`）: **IFC を見ない唯一の解析モジュール**で、member / column 命令の
  平面ジオメトリだけから「受ける材のある端部」を判定する。平行（継ぎ手・側並び）・別レイヤ・
  Z 分離は除外し、登り梁のときだけレイヤ一致を外す。配置先は横架材自身のレイヤ。
- ✅ `draw/Symbol`: 4 種を**1 本の描画**にまとめた（`VWSymbolObj(name, VWPoint2D, angleDeg)`＝
  VS の `vs.Symbol` にあたる。ci-debug の `sdk-grep` / `sdk-ls` で実在確認済み）。命令型も
  `core::SymbolCommand` 1 つに統合してある（理由は `core/Document.h` の doc コメント）。
  **配置そのものが唯一の門**で、`VWSymbolObj` の構築を必ず試みる。当初は
  `VWSymbolDefObj::IsSymbolDefObject` で先に弾いていたが、その判定が期待どおりでないと
  **1 つも置けないうえに原因を誤って指す**ため事前ガードを外した。失敗したときだけ
  `IsSymbolDefObject` を引いて、「配置先レイヤが無い」「図面にシンボル定義が無い」
  「定義はあるのに配置できない」を完了ダイアログの診断行へ書き分ける（プラグインは
  シンボル定義を作らない）。
- ✅ **［VW 2026 SDK の落とし穴］`VWSymbolObj` には 2 つの作法が要る。** ISDK には
  シンボルを配置する呼び出しが無く（在るのは `CreateSymbolDefinition` だけ）、この構築子は
  レガシーの `PlaceSymbol` を包んでいる。どちらも**外すと静かに壊れる**ので、`draw/Symbol` の
  冒頭にも同じことを書いてある。
  1. **生成しただけでは図面に現れない。** VWFC の他のラッパー（ポリライン等）と違い、
     できたインスタンスはアクティブレイヤに入らない。生成後に
     `gSDK->AddObjectToContainer(handle, layer)`（「h を container の末尾へ**移動**する」）で
     配置先レイヤへ入れ直す。
  2. **非 nil のハンドルを成功判定に使わない。** `PlaceSymbol` は「定義が nil なら何もしない」
     仕様で、構築子は何も置けていなくても非 nil のハンドルを返し得る。
     `VWSymbolObj::IsSymbolObject(handle, name)` まで確かめてから 1 件と数える。

  どちらも「シンボルがひとつも配置できない」というローカル実機の報告から切り分けたもの。
  定義（テンプレート由来）・レイヤ・名前・座標がすべて正しいのに 4 種 472 件が図面に現れず、
  しかも完了ダイアログは**全数成功で嘘をついていた**（2 が無かったため）。
- ✅ テスト: `ParseAnchorBoltTests` / `ParseFloorPostTests` / `ParseFireBraceTests` /
  `ParseJointTests`（Python 版 `test_ifc_{anchor_bolt,floor_post,fire_brace,joint}.py` の
  全ケースを移植）。`CoreDocumentTests` にシンボル命令の検証、`ParseContextTests` に
  コンテキスト経由と従来経路の一致を追加。

- ✅ **基礎ストーリへ M11 のレベルを 2 つ足した**（`parse/Footing` の
  `buildFoundationStoryCommand`）。M9 は描画対象のある GL・底盤天端の 2 レベルだけを作って
  いたが、M11 でアンカーボルト・床束が入ったので**基礎天端**（立上り天端の絶対 Z。
  `F-アンカーボルト`）と**床束**（底盤天端に揃える。`F-床束`）を挿し込み、スタック順は
  Python 版と同じ 基礎天端 → GL → 床束 → 底盤天端 の 4 つになった。立上りの天端は
  `resolveFoundationTopElevation`（立上りの天端 Z の最大値。立上りが無い基礎は底盤天端へ
  フォールバック）。実フィクスチャの基礎天端は 300〜400mm、床束＝底盤天端は −300〜50mm。
- ✅ レイヤ名の定数（`kLayerFoundationAnchor` / `kLayerFoundationFloorPost`）は M9 の
  `kLayerFoundationWall` / `…Slab` と並べて **`parse/Footing.h` に 1 つだけ**置く。配置先を
  名乗るシンボル側と、レベルを作るストーリ側の両方がこれを通る。

**ローカル確認: ✅ 済み。** 火打・仕口が正しい位置・角度・レイヤ（横架材天端／軒高／母屋／
登り梁）に置かれること、**鋼製火打の 45 度補正**（Python 版がシンボルの基準姿勢に合わせて
足している値）が VW 2026 のシンボルでも正しいこと、**仕口の向き**（端部から部材内側へ）が
意図どおりであること、アンカーボルト・床束が `F-アンカーボルト`（基礎天端＝立上り天端）・
`F-床束`（底盤天端）のレイヤに基礎ストーリの高さで載ることを実機で確認した。
なおシンボル定義（`アンカーボルト_M12/_M16` / `床束` / `鋼製火打` / `仕口`）はプラグインが
作らないので、図面に無ければ完了ダイアログの診断行に名前が出る。

---

## M12 — 断面記号・伏図記号 ✅

**目的:** 柱・小屋束の断面記号（×／／）と伏図記号（平面記号）を描く。
**Python: `ifc/column_mark.py` / `vw/column_mark.py`。**

### 【決定】記号は PIO で描き、PIO は本プラグインに同梱する

記号を素のジオメトリ（直線）や VW 同梱のデータタグで描くことも検討したが、**柱の断面が
変わったときに追随できず、間違った断面の記号が残る**——記号が無いより悪い。したがって
Python 版と同じく PIO で描く。ただし姉妹プロジェクトの `柱束伏図記号` を使うのではなく、
**本プラグインのモジュールへ拡張として同梱する**（`Extensions/ExtColumnMark`）:

- インストールも自動アップデートも 1 つで済む（`scripts/vw-update.*` はここにしか無い）
- 記号の規約（構造用途 4/5・span レイヤ名・クラス名・シンボル名）を解析側と 1 か所で共有
- インポータと PIO のバージョンずれが起きない
- 登録名は `HomeskzColumnMark` として Python 版と分ける（両方入れた環境で衝突しない）

**PIO 依存の代償は小さい**ことを実機で確認した: プラグインを外して過去の図面を開いても、
**PIO が描いたジオメトリは保存されていて表示できる**（更新ができないだけ）。当初の懸念
「未インストール機で表示が崩れる」は起きない。

なお追随は**リセットが契機**（柱を動かした瞬間ではない）。それでも、リセット・ファイル
再オープンのたびに実物から導き直されるので、**嘘の記号が残り続けることはない**——静的な
ジオメトリやサイズ別シンボルとの決定的な違いはここにある。

- ✅ `parse/ColumnMark`: 実在する span 柱レイヤごとに断面記号 1 つ・伏図記号 1 つの
  `ColumnMarkCommand`（配置先・作図クラス・検索対象レイヤ／クラス・記号スタイル・
  シンボル）。柱の本数には依存しない（記号は PIO が対象レイヤを検索して描く）。
- ✅ `draw/ColumnMark`: PIO を作りパラメータを書いて `ResetObject`。伏図記号レイヤは
  ストーリが作らない独立レイヤなので無ければ作る（通り芯 `共通` と同じ）。
- ✅ `Extensions/ExtColumnMark`: **PIO 本体**。`Recalculate` で対象レイヤの構造材を走査し、
  構造用途 4/5 のものを実断面（`MajorBreadth`/`MajorDepth`）から ×／／ で、平面記号は
  シンボルで描く。`ModuleMain` がメニューコマンドと並べて登録する。
- ✅ **M13 の伏図へ伏図記号レイヤの表示を足した**（切断位置の直下＝`to` < 切断で最大の `to`
  を 1 枚）。断面記号（span レイヤ）と**排他**になり、同じ柱が両方の記号で出ることはない。
  伏図記号レイヤは `reorderStoryLayers` の `topLayers` として `共通` の直下へ積む。
- ✅ テスト: `ParseColumnMarkTests`（span ごとに 2 つ・配置先と検索対象・クラス・シンボルの
  選択・並び・レイヤ列挙と「切断の直下」・実フィクスチャ通し・決定性）。
  `CoreDocumentTests` に検証ケース。

**ローカル確認: 済み。** 各 span レイヤに断面記号（柱＝×・小屋束＝／）が実断面の大きさで
載り、伏図記号レイヤに平面記号が出て、伏図（M13）では切断の直下の 1 枚だけが出ることを
実機で確認した。**シンボル `柱伏図記号` / `束伏図記号` は図面側の用意**（プラグインは作らない）。

### 実機でしか出なかった落とし穴（記号 PIO を足すときはここを踏む）

CI が全て緑でも次の 3 つは通ってしまい、実機で初めて出た。**PIO を新しく足すときは
最初からこの 3 つを入れる。**

1. **生成のたびに「オブジェクトの設定」ダイアログが出る。** `CreateCustomObject` は、その
   名前の PIO が文書に未定義なら `DefineCustomObject` で定義を作り、その `prefWhen` の
   **既定が `kCustomObjectPrefAlways`**。そのため**最初の 1 個のときだけ**ダイアログが出て
   インポートが止まる（2 個目以降は定義済みなので出ない）。PIO 側の `OnInitXProperties` で
   `kObjXPropShowPrefDialogWhen` を宣言しても**定義が作られる過程で走るので 1 回目に
   間に合わない**——描画側で作り始める前に
   `DefineCustomObject(name, kCustomObjectPrefNever)` を呼んで先に定義しておく。
2. **PIO のジオメトリは PIO 自身のローカル座標で持たれる。** 走査して見つけた対象の
   **ワールド座標へそのまま描いてはいけない**。PIO を動かした量だけ絵がずれ、しかも
   リセットしても同じ相対位置に描き直すので直らない（`ResetOnMove` を立てても解決しない）。
   `GetObjectToWorldTransform` ＋ `InversePointTransform` でローカルへ落としてから描く
   （回転も戻るので PIO を回しても絵は対象の上に残る）。
3. **リセットの契機は自分で宣言する。** 既定では何も設定されておらず、印刷でも再オープンでも
   描き直されない。`ResetOnMove` / `ResetOnRotate` と `kObjXPropResetBeforeExport`
   （印刷・書き出しの直前）を立てる。

### 【限界】柱を動かした瞬間には追随しない

**「他の図形が変わったら再計算する」仕組みは VW に無い。** `kObjXProp*` を全数確認したが、
リセット系はすべて PIO 自身に関するもの（自分のレコード・自分のパス／プロファイル・
ビューポート倍率・書類単位・書き出し前）だけで、association にも**追加**の公開 API が無い。
したがって追随の契機は上記 3 のものに限られる。**Python 版（姉妹プロジェクトの
「柱束伏図記号」）も同じ**で、README・CLAUDE.md とも「柱が編集されれば**オブジェクトの
リセットで**追随する」と書いてある（あちらはスクリプト PIO なのでリセット条件を
プラグインエディタの GUI で持っており、リポジトリには現れない）。

実用上は `kObjXPropResetBeforeExport` が効く——**図面として外へ出る瞬間には必ず実物と
一致する**。画面上で即座に合わせたいときは記号を選んでリセットする。図面内の記号を
一括で更新するメニューコマンドは、要望が出たら足す（使われない枠を先に作らない）。

---

## M13 — シート・伏図・データタグ・凡例 🟨

**目的:** シートレイヤとビューポート（基礎伏図／各階柱梁伏図／母屋伏図）、断面寸法データタグ、
グラフィック凡例。**Python: `ifc/sheet.py` / `ifc/tag.py` / `vw/sheet.py`。**

- ✅ `parse/Sheet`: 基礎伏図（基礎があるときだけ）・各階の柱梁伏図・屋根版を持つ階ごとの
  母屋伏図の `SheetCommand`/`ViewportCommand`。**切断レベル**（柱梁伏図＝床レベル + 0.25 /
  母屋伏図＝ + 0.75）を span が含む柱レイヤだけを表示レイヤに載せる。番号は
  基礎伏図 `1` → 柱梁伏図 `2`… → 母屋伏図の順。
- ✅ `draw/Sheet`: シートレイヤ（番号＝レイヤ名・タイトル）／ビューポート生成、表示レイヤの
  絞り込み（全隠し → 命令のレイヤだけ表示）、縮尺（表示レイヤに合わせる）、図面タイトル・図番、
  更新。
- 🟨 **レイヤの重ね順（M3 の【決定】の実装箇所）**: `draw/Story` の `reorderStoryLayers` が
  `InsertObjectAfter` で**デザインレイヤの並びそのもの**を希望順（`core::desiredStoryLayerOrder`）
  へ組み替える。**ビューポート生成より前**に行うので、Python 版の「並べ替えが既存ビューポートに
  反映されない」制約は起きない。**per-viewport の上書きは実機で効かなかったため捨てた**
  （呼び出しは true を返すのに図面に 1 件も記録されない。M3 の【決定】参照）。動かせた数が
  0 件なら完了ダイアログの診断行に出す。
- ✅ テスト: `ParseSheetTests`（タイトル・切断レベルによる span 柱レイヤの絞り込み・番号の
  連番・基礎の有無による出し分け・母屋伏図を作る階・**表示レイヤがストーリの作るレイヤに
  実在すること**・決定性）。`CoreDocumentTests` に sheet の検証ケースを追加。
- ⬜ **伏図記号レイヤ（`{to}-柱伏図記号`）の表示**: 切断位置の直下（`to` < 切断で最大の `to`）を
  載せる。そのレイヤを作るのは **M12** なので、M12 が着地したら `parse/Sheet` の TODO
  （柱梁伏図・母屋伏図の 2 か所）へ足す。レイヤ名の規約は M12 側（`parse/ColumnMark`）が持つ。
- ⬜ `parse/Tag` / 凡例: 断面寸法データタグ（`Data Tag`＋関連付け＋ビューポート注釈。横架材
  ハンドルは命令インデックス → ハンドルの対応表で受け渡す）とグラフィック凡例
  （`GraphicLegend`＋スタイル）。命令（`TagCommand` / `LegendCommand`）ごと未着手。
- 🟨 **クラスの表示**: **ビューポートはクラスの表示を明示しないと非表示のまま**（stable での
  ローカル確認で判明。レイヤは命令どおりでもクラスが全部消えていた）。**ISDK にドキュメントの
  全クラスを列挙する呼び出しが無い**（`VWClass` は名前↔索引の変換だけ・`BuildResourceList` に
  クラス用の型も無い）ため、**図形が身に付けているクラスを全レイヤ走査で数え上げて**
  （`CollectUsedClasses`。**PIO / シンボルの中まで辿る**＝通り芯ラベルのようにスタイルが
  決めるクラスを拾うため）1 つずつ表示へ戻す。命令セットが名乗るクラス
  （`core::documentClassNames`）も取りこぼし防止に足す。**`SetUseDocumentClassVis`（オブジェクト
  変数 1031）は使えない**——但し書きが "for dlvps"（デザインレイヤビューポート専用）で、
  1 回目の修正では効かなかった。Python 版の `hidden_classes`（クラス単位の非表示）は**どの伏図でも
  使われていない**ので命令には持たせない（クラスで絞る伏図が要るときに足す）。
- 🟨 **重ね順は方式ごと差し替えた**（上の項目）。2 回のローカル確認で per-viewport 上書きは
  「設定 33 件・図面に記録 0 件」＝**呼び出しは成功を返すのに残らない**と分かったため、
  デザインレイヤの並べ替え（`InsertObjectAfter`）へ切り替えた。

**ローカル確認（1・2 回目・済）:** 伏図の枚数・番号・**表示レイヤは正しかった**。
（a）**クラスが全て非表示**だった件は、図形が使うクラスを走査して数え上げる方式で**解消**
（通り芯ラベルも出るようになった）。（b）**レイヤの重ね順**は per-viewport 上書きでは
どうやっても効かず（「設定 33 件・図面に記録 0 件」）、デザインレイヤの並べ替えへ方式を
差し替えた（3 回目の確認待ち）。

**ローカル確認:** 各伏図がシートに並び、表示レイヤ・**クラス**・縮尺が正しい（**伏図の枚数・
番号・図面タイトル**＝基礎伏図 1・各階の床/小屋伏図・屋根版のある階の母屋伏図）。
**伏図で床・野地板が柱・梁を覆っていない**（重ね順上書きが効いている）。柱の断面が切断
レベルどおりに出る（下屋の小屋束が上階の小屋伏図に写り込まない／母屋伏図に小屋束の断面が
出ない）。断面寸法タグと凡例は M13 の残りなのでまだ載らない。

---

## M14 — 断面ビューポート（軸組図） 🟨

**目的:** 柱梁の芯を通る通りごとに断面ビューポート（軸組図）をシートレイヤ `A` へ並べる。
**Python: `ifc/section.py` / `vw/section.py`。**

**【決定】断面ビューポートは新規作成する（Python 版との最大の差）。** Python 版（VectorScript）は
断面ビューポートを作れないため、`X1`〜`X20`/`Y1`〜`Y20` の **40 枚を手で用意しておき**、その
指示線を探して動かし・改名し・余りを削除する、という運用で凌いでいた。C++ SDK には
`ISDK::CreateSectionViewport(pt1, pt2, pt3, depth, startHeight, endHeight, layer)` があるので
（ci-debug の `sdk-grep` で確認）、本移植は**検出した通りの数だけその場で作る**。結果として

- 図面テンプレートに 40 枚を仕込む下準備が要らない、
- 既製枚数の上限（Python 版は方向あたり 20 本まで）が消える、
- 流用元の図番（`source_number`）・未使用ぶんの削除・指示線の検索が丸ごと不要になる。

代わりに、既製ビューポートが持っていた情報を**解析側が決めて命令に載せる**必要がある:
視線の向き（`viewPoint`）・奥行き（`depth`）・断面の高さ範囲（`startHeight`/`endHeight`）・
映すデザインレイヤ。

- ✅ `parse/Section`: 柱梁の芯を X/Y 方向に検出（**柱と、その通りに平行な梁の両方**が要る。
  大引・母屋は梁とみなさない）、命名（名前付き通り芯・中間通りの `'`/`又`）、指示線の 2 点＋
  視線の向き・表示レイヤ → `SectionCommand`。
- ✅ `draw/Section`: `CreateSectionViewport` で 1 枚ずつ作り、表示レイヤ・クラス・縮尺・
  図番・タイトルを設定して更新し、シートレイヤ上で重ならないよう格子状に並べる
  （実寸は `GetObjectBounds` で測る。Python 版 `_arrange_viewports` と同じ）。
- ✅ **ビューポート共通の手当ては `draw/DrawUtil` へ集約**（`PrepareViewportSetup` /
  `PrepareSheetLayer` / `ConfigureViewport`）。伏図（M13）と軸組図で逐語的に同じ手順
  （シートレイヤ用意 → 表示レイヤを全部隠して命令のぶんだけ戻す → クラスを 1 つずつ表示へ →
  縮尺 → 図番・タイトル → 更新）だったため。`CreateSectionViewport` のヘッダにも
  「クラス・レイヤの表示はこの呼び出しでは扱わない。呼び出し後に設定し、そのあとで更新する
  こと」と明記されており、断面でも同じ後処理でよい。
- ✅ テスト: `ParseSectionTests`（切断位置の検出・大引/母屋の除外・命名・指示線の向きと長さ・
  視線の向き・高さ範囲・表示レイヤの実在・決定性）。`CoreDocumentTests` に section の検証ケース。

- ✅ **断面の範囲と表示の作法**（1 回目のローカル確認を受けて追加）:
  * **範囲**（2・3 回目のローカル確認で挙動が確定）: `CreateSectionViewport` の 3 つの数値引数が
    範囲を決める唯一の手段で、**「無限」に切り替える呼び出しもオブジェクト変数も SDK に無い**
    （断面まわりの API は `CreateSectionViewport` / `CreateSectionLineInstance` /
    `IsSectionLineLinkedToViewport` / `UpdateSectionLineInstances` の 4 つだけ。
    `ovSectionViewport*` にも範囲の項目は無い。ci-debug で SDK 全体を検索）。実測では
    **`depth` = 0 → 〈切断面より奥の範囲: 無限〉**（＝奥行きだけは 0 が無限）だが、
    **`startHeight` / `endHeight` = 0 → 〈高さの範囲: 有限・0〜0〉**になってしまい、断面から
    建物が消えてしまう。したがって
    **奥行き＝0（無限）／高さ＝建物を包む実寸＋余白（`core::sectionHeightRange`）／
    長さ＝断面線の長さ**（指示線を通り芯 bbox + `kSectionLineMargin`＝3000mm まで延ばして
    実質無制限にする）とする。
  * **【調査済・打ち切り】長さ・高さを文字どおり「無限」にはできない。** UI で手作りすると
    〈無限〉が既定なので「公開ヘッダに載っていないオブジェクト変数（欠番 1060〜1090）に
    範囲が載っているのでは」と疑い、dev ビルドに読み取り専用の一時診断を入れて実機で確かめた
    （手で 1 枚だけ〈無限〉に変えてから再インポートし、値の違う変数を探す）。結果は
    **断面ビューポート 66 枚・値の組み合わせ 1 通り＝差のある変数は 1 つも無し**。範囲は
    オブジェクト変数の外に保持されており、SDK からは触れない。**診断コードは役目を終えたので
    削除し、建物の大きさから決めた有限範囲で確定とする**（実用上は無限と同じ見え方になる）。
  * **切断面より奥は表示しない**（オブジェクト変数 **1064**
    `ovSectionViewportDisplayObjectsBeyondCutPlane` = false）。
  * **プレイナー（アクティブレイヤ平面）図形は表示しない**（**1035** `ovViewportDisplayPlanar`
    = false）。
  * **2D コンポーネントは表示する**（**1059** `ovViewportDisplay2DComponents` = true。既定でも
    表示だが、既定値に頼らず明示する）。
  いずれも**ビューポートの更新より前**に設定する（`CreateSectionViewport` のヘッダコメントが
  「表示設定は呼び出し後、更新はその後」と明記）。**どこを切るかではなく描き方**なので、
  値は `draw/Section.cpp` の名前付き定数が持ち、命令セットには載せない。

**【1 回目のローカル確認・済】** 断面指示線は直線（＝**pt3 は「見る側」を示す点**という
読みで正しかった）。視線の向きも意図どおり（X通りは い→ろ、Y通りは X1→X2 が左から右）。

**【2 回目のローカル確認・済】** 高さの範囲が〈有限・0〜0〉、長さの範囲が〈断面線の長さ〉に
なっていた（＝高さには 0＝無限の解釈が効かない）。上記のとおり高さは実寸＋余白を渡す形へ
変更し、長さは指示線を伸ばして補う。

**【3 回目のローカル確認・済（範囲の探索）】** 手で 1 枚を〈無限〉にしてから再インポートしても
オブジェクト変数に差は出なかった（上記【調査済・打ち切り】）。**範囲は建物の大きさで決める**
方針で確定し、一時診断は削除した。

**ローカル確認（4 回目）:** 軸組図シート（`A`）に、柱梁が通る各通りの断面ビューポートが
図番・タイトル付きで重ならずに並ぶ。図番は通り芯名（中間通りは `X1'` / `又い`）。
**切断面より奥の軸組が写っていない**・**プレイナー図形が出ていない**・**2D コンポーネントは
出ている**。**断面が建物の上下・左右で切れていない**（高さ＝実寸＋余白 1000mm・長さ＝通り芯
bbox + 3000mm が建物を覆っているか）。

---

## M15 — 仕上げ（実行 UX・クラッシュ診断） 🟨

**目的:** 要素の移植で後回しにしてきた**実行体験と診断**を整える。Python 版 `run()` /
`tracing.py` に相当し、どの要素にも属さないため最後にまとめる（要素移植を止めない）。
ネイティブは Python 版と違って**例外・クラッシュが VW を巻き込む**ため、診断の価値は
Python 版より高い。

- ⬜ **完了文言の集約**: `Extensions/ExtMenu.cpp` に直書きしている結果メッセージを、無 SDK 側の
  **`parse/Summary` へ転用**して組み立てる（`summarizeIfc` は M1 で `buildDocument` に
  置き換わって以降未使用。命令数 → 文言の整形は純ロジックなので `ParseSummaryTests` で
  そのままテストできる）。マイルストーンごとに SDK 側の文言を手で伸ばす作業が無くなる。
- ✅ **進捗表示**（他の項目に先行して実装。「インポート中にフリーズしたように見える」という
  実使用の指摘を受けたため）: `core/Progress`（SDK 非依存の報告先＋文言整形＋配分）と
  `draw/ProgressDialog`（`VWFC::Tools::CProgressDlg` への橋渡し）を追加し、`buildDocument` /
  `executeDocument` の両方が同じ報告先へ「いま何を・何件中の何件まで」を報告する。
  **1 件ごとに `DoYield` する**ので、進み具合が見えるだけでなく VW の再描画とイベント処理が
  戻り、キャンセルも効く（中止時は描けたところまで残り、完了ダイアログがその旨を出す）。
  ステータスバー（Python 版 `vs.Message`）ではなく進捗ダイアログにしたのは、ISDK に
  `vs.Message` 相当が無い一方で、進捗バーとキャンセルを備えた `CProgressDlg` があるため
  （ci-debug の `sdk-grep` / `sdk-ls` で確認）。**ローカル確認済み**: 進捗が表示されること、
  1 件ごとの `DoYield` のオーバーヘッドが体感で問題にならないこと（＝間引きは不要）。
  **残り**: 配分の重み（現状は命令数の比。実測で 1 件あたりの重さが要素ごとに大きく違うと
  分かれば見直す）と、完了報告をモーダルのままにするかの判断。
- ⬜ **例外処理**: `parse`/`draw` の失敗を SDK コールバックへ漏らさず、ユーザーへ 1 通の
  エラーダイアログとして見せる（CLAUDE.md「エラーハンドリング」）。1 要素の欠損で全体を
  止めない現在の寛容さは維持する。
- ⬜ **`core/Trace`（任意）**: Python 版 `tracing.py` 相当のフェーズ単位ログ。無言クラッシュ時に
  「ログ最終行の直後」が原因箇所になる形で、`buildDocument`／各 draw フェーズの前後に置く。
  出力先・有効化条件（dev ビルドのみ等）を決める。
- ⬜ **Undo の一括化**（要調査）: インポート全体が 1 回の Undo で戻せるか確認し、SDK に
  相当する仕組みがあれば適用する（Python 版には無い改善）。
- ⬜ テスト: 文言整形は `ParseSummaryTests`。トレース・UX は無 SDK にできる範囲でテストし、
  残りはローカル確認。

**ローカル確認:** 解析中・描画中の進捗が見え、完了時に全要素の件数が読める。IFC が壊れて
いてもプラグインが VW を落とさずエラーを 1 通で報告する。インポートを Undo で戻せる
（一括化した場合）。

**ローカル確認（進捗表示。先行実装ぶん）:**

- インポート開始と同時に進捗ダイアログが出て、上段にファイル名、メーター横に
  「横架材を描画しています… (12/196)」のような見出しと件数が出る。
- 描画中もダイアログが描き換わり続ける（＝固まって見えない）。VW 自体の応答も戻っている。
- 見出しが ストーリ → 通り芯 → 床 → 横架材 → 垂木 → 野地板 の順に進み、バーが最後まで届く。
- キャンセルを押すと速やかに止まり、完了ダイアログに「キャンセルされたため、途中で
  中断しました。」が出る。そこまでに描いたオブジェクトは図面に残っている。
- 完了ダイアログが出るときには進捗ダイアログが閉じている（2 枚重ならない）。
- ~~進捗ダイアログの yield が重すぎてインポート全体が遅くなっていないか~~ → 確認済み
  （体感で問題なし。「n 件ごとに yield」への間引きは不要）。

---

## 完了後

- 全フィクスチャで `buildDocument` と描画を通し、Python 版との**意図的な差分**を
  「なぜそうしたか」とともに 1 か所へまとめる（下記の記録項目と合わせる）。
- README を移植版の内容へ更新（ビルド・使い方・Python 版との差異）。
- Python 版で未実現／トリッキーだった箇所のうち、C++ SDK でより良く実装できたものを記録
  （屋根＝屋根面オブジェクト直組み／床＝スラブ化／レイヤ重ね順＝per-viewport 上書き 等）。

## 順序の要点（依存関係）

**形状（床・屋根面）を先に確定し、支持部材（横架材・柱）をそれに合わせる。**

```
M0 基盤 ─┬─ M1 通り芯（縦切り実証）
         └─ M2 幾何土台 ── M3 ストーリ ── M4 構造クラス
                                │
              ┌──── 形状先行 ────┴──── 形状先行 ────┐
           M5 床板                        M6 屋根面（垂木・野地板）
              │                                    │  屋根面を確定
              └──────────────┬─────────────────────┘
                             │  形状へ支持部材を合わせる
                   M7 横架材（母屋・棟木・登り梁＝屋根面へスナップ補正）
                             │
                   M8 柱（span・小屋束／登り梁端部を柱面へ最終化）

  基礎（上部形状と独立・M7/M8 と並行着手可）
  M9 基礎(壁/スラブ) ── M10 基礎(高度)

  記号・伏図系（部材・柱の確定後）
  M11 シンボル ── M12 断面記号/伏図記号 ── M13 伏図/タグ/凡例 ── M14 軸組図

  最後に（どの要素にも属さない）
  M15 仕上げ（実行 UX・診断）
```

依存の要点:

- **M6 屋根面 → M7 登り梁**: 登り梁は M6 で確定した屋根面へスナップ補正する（形状 → 支持部材）。
- **M7 横架材 → M6 垂木の精緻化**: 垂木の差し込みが参照する軒桁幅は、M6 では既定値
  105mm、M7 で実寸へ切り替えた（逆向きの後追い依存はこの 1 点だけ。実装済み）。
- **M7 横架材 → M8 柱**: 柱の span（to レベル）判定は上階梁の下端を参照し、小屋束の断面は
  直上に乗る母屋・棟木・登り梁の幅に合わせる。
- **M8 柱 → M7 登り梁の最終化**: 登り梁端部の食い込み解消が柱面まで届く（M7 は横架材のみ）。
- **M7/M8 → M11 仕口・M12 記号・M13 伏図**: 端部判定・span レイヤ・タグ関連付けは部材／柱に依存。
- **M9/M10 基礎**は上部形状と独立で、M7/M8 と並行に着手してよい。M9 の自由端の柱芯合わせと
  M11 のアンカーボルト・床束だけが基礎側から柱・大引を参照する。
- **M3 の【決定】→ M13**: デザインレイヤの重ね順は変更せず、M13 の per-viewport 上書きで
  目的を満たす。M5/M6/M12 はこの決定を参照するだけ。

各要素の詳細仕様は姉妹リポジトリ `vectorworks-plugin-script-import-ifc-homeskz` の
`CLAUDE.md` および該当ソース・テストを一次資料とする。
